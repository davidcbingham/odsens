# Open Questions (running list)

Answered items get folded into `spec.md` and struck here.

## Content & ownership
1. ~~**Source of truth for projects**~~ — **Answered:** Everything on Modrinth appears on the site, **plus exclusive on-site projects** (Oliver authors name/description/gallery and uploads the file). Modrinth parameters = the schema. Four categories: mod, datapack, resource pack, plugin.
2. ~~**CurseForge overlap**~~ — **Answered:** Little content on CF, all also on Modrinth. **Get an API key**; sum download counts across both. Placeholder in `.env`.
3. ~~**Scratch games and YouTube**~~ — **Answered:** **No Scratch** on the site. YouTube: yes (embed).
4. ~~**Skins**~~ — **Answered:** He doesn't use any skin site. **Native Skins section** — design deferred to a dedicated discussion.
5. ~~**Non-Minecraft work**~~ — **Answered:** See #1.
6. ~~**Blog / posts / devlogs**~~ — **Answered:** Deferred, maybe never.

## Editing workflow
7. ~~**How does Oliver edit content?** Options: (a) a small admin UI in the site (login → forms) — most friendly; (b) markdown/JSON files in the repo edited via GitHub — teaches git, no UI to build; (c) Supabase dashboard directly — zero build, but clunky. Preference?
8. ~~**Will Oliver code?** Is part of the goal for him to learn web dev on this project (affects framework choice and how much we document), or should it be a finished tool he uses?

## Comments & moderation
9. ~~**Moderation model**~~ — **Answered:** See earlier batch — admin setting; multi-mod; notifications toggle.
10. ~~**Replies / threads / reactions**~~ — **Answered:** **Threaded comments + likes.** Threads let Oliver reply to people; likes surface feedback on what resonates. (Updated 2026-08-16.)
11. ~~**Notifications**~~ — **Answered:** Yes, but **channels + infrastructure need a dedicated design session** (email / Discord / in-app / push; per-event types). Added to Future Design Sessions.

## Donations
12. ~~**Platform**~~ — **Answered:** **Ko-fi**, with **maximum usability, minimum clicks** — embedded panel, no redirect where avoidable. Also: 12b **Custom Orders / commissions** (leverage Ko-fi Commissions), 12c **donor leaderboard** (idea queue).

## Identity & aesthetic
13. ~~**Name presentation**~~ — **Answered:** **OddSense** = Oliver (his username on the site) and his Minecraft character. **odsens** = the website/brand — distinct from the person and character.
14. ~~**Existing art assets**~~ — **Answered:** Yes. Oliver will work with Claude in a separate **Claude Design** session to produce a `DESIGN.md`. Process + advantages documented in `docs/design-process.md`.
15. ~~**Tone**~~ — **Answered:** **Playful and cartoony. Fun, relaxed, inviting.**
16. ~~**Reference sites**~~ — **Answered:** No named references. After the functional spec is complete: abstract it into site/functionality types → Claude searches for examples → Oliver critiques good/bad. Process in `docs/design-process.md`.

## Technical / accounts
17. ~~**Framework**~~ — **Answered 2026-08-17:** **Next.js (App Router, TS) on Vercel + Supabase**, plain CSS tokens (no Tailwind), Resend for email. Full reasoning in `docs/framework-decision.md`.
18. ~~**Google OAuth**~~ — **Answered:** Google Cloud project under **david@studiobing.com**; hosts both the OAuth client and the YouTube API key. Step-by-step in `docs/setup-google-cloud.md`.
19. ~~**Vercel/Supabase org access**~~ — **Answered:** Discussed 2026-08-16 — see tradeoffs below. **Recommendation:** start with StudioBing logins only; Oliver operates through GitHub + the admin UI (Vercel auto-deploys from GitHub). Add him to Vercel/Supabase later only if needed.
20. ~~**Analytics**~~ — **Answered:** Options + effort table in `docs/analytics-options.md`. Recommend Vercel Web Analytics + Speed Insights + custom events + own Supabase counters at launch; daily external-stats snapshots as first enhancement.
21. ~~**Squarespace DNS**~~ — **Answered:** Confirmed: registration only. Clean sheet; old Cloudflare project is deprecated.

## Privacy
22. ~~**What personal info is OK to publish?** First name? Age? Nothing beyond the handle? (Scratch bio currently says "I'm 15 and in 9th grade.")

## New (from 2026-08-16 answers)
23. ~~**Content without a native home**~~ — **Answered:** **No PII on the site.** Applies to visitors too: on first sign-in every user must **choose a handle** (+ optional profile image); Google name/email are **never** displayed or used as a display name. Oliver's own presence = handle only.
24. ~~**CurseForge API key**~~ — **Answered:** Yes — key goes in `.env` as `CURSEFORGE_API_KEY`.
25. ~~**YouTube Data API**~~ — **Answered:** Yes — same Google Cloud project as OAuth. See `docs/setup-google-cloud.md`.
26. ~~**Roblox**~~ — **Answered:** No Roblox.
27. ~~**Oliver's setup**~~ — **Answered:** VS Code + Claude Code working on his laptop; has a GitHub account (unused). David will set up the clone and give **full repo access** to both. David front-loads effort, then transitions to Oliver with minimal maintenance pushes. Skills may expand to cover major updates, design decisions, dev work.

## Future design-detail sessions (agreed, not yet scheduled)
- **Skins section** — how skins are shown, viewer, download, metadata (from Q4)
- ~~**Notifications**~~ — **designed 2026-08-17** → `docs/notifications.md`
- **Comments** — threaded + likes UX, notification hooks, moderation UX (from Q10)
- **Custom Orders + Workrooms** — intake (designed), workroom flows, safety rules, retention, client email opt-in (Phase 2)
- **Asset IA** — categories, file types, pixel dimensions for `assets/brand/` (from Q14)

## Idea queue (not committed)
- **Donor leaderboard** — ranks supporters by total tipped; **ties to site account names** (Google-authenticated users). Needs a way to link a Ko-fi payment to a site account (e.g. Ko-fi webhook `email`/`from_name` match, or a "link my Ko-fi" step / code in the tip message). Privacy: opt-in display name.
- **Custom Orders** — see design session above.

## Post-spec workstreams (agreed 2026-08-16)
- ~~**DESIGN.md** via Claude Design session with Oliver~~ — **done** (`/DESIGN.md` v1.1, Crate Poster). ~~Review pass~~ **done** → `docs/design-review.md`; gaps logged as Q28–32.
- **Inspiration hunt** from abstracted functionality → `docs/design-process.md`
- ~~**Site management skills**~~ — **spec'd 2026-08-17** (12 skills mapped to Oliver's moments) → `docs/site-management-skills.md`; written for real once the first build exists
- ~~**Framework choice**~~ — **done** → `docs/framework-decision.md`

## From the DESIGN.md review pass (2026-08-16) — see `docs/design-review.md`
28. ~~**Card border contrast**~~ — **Answered (pass 2):** Card/panel outlines lifted one step to `--line-soft #2C3A4B`; `--line` kept for internal dividers.
29. ~~**User notifications**~~ — **Answered (pass 2):** **Cut from v1** — no user inbox; admin gets email. Bell spec retained for later.
30. ~~**YouTube Shorts**~~ — **Answered (pass 2):** **Shorts row added** under the long-form grid (9:16 facades).
31. ~~**Hero**~~ — **Answered (pass 2):** Unchanged in pass 2 (featured-project takeover hero) — treated as confirmed.
32. ~~**Second Claude Design pass**~~ — **Answered (pass 2):** **Done 2026-08-17** — `design/claude-design-export/pass-2/`, `DESIGN.md` v1.2 §11.
## From pass 2 (Claude Design's open items, 2026-08-17)
33. ~~**Ko-fi tip event**~~ — **Answered 2026-08-17:** v1 supporters wall = **leaderboard: handle + amount**. Linking a tip to a site account: server-side **hashed-email match** (never displayed/stored raw) → else handle typed in the Ko-fi message → else "Anonymous · $X". Amount shown only when linked or Ko-fi `is_public`. DESIGN.md note updated (was "no amounts").
34. ~~**Handle validation heuristic**~~ — **Answered 2026-08-17:** **No name detection.** Structural validation only (3–20, `[A-Za-z0-9_]`, unique, reserved words, no `@`); heavy guidance copy at handle creation + in the privacy/comments pages. Design's "looks like a real name" state dropped.
35. ~~**Comment limits**~~ — **Answered 2026-08-17:** Yes — 1000 chars, one link per comment.
36. ~~**Under-13 line** on the Privacy page ("don't sign in") — needs David's call before ship.
37. ~~**Still-missing art**: project icons, in-game screenshots, rendered 3D skin busts (build-time via skinview3d).

## From data-model design (2026-08-17) — see `docs/data-model.md`
38. ~~**Auto-hold on reports**~~ — **Answered 2026-08-17:** Yes — auto-hold at ≥3 reports.
39. ~~**CurseForge id mapping**~~ — **Answered 2026-08-17:** Yes — manual CurseForge id entry in admin.
40. ~~**Comment edit window**~~ — **Answered 2026-08-17:** Yes — 15-minute **edit** window after posting; then delete only.

## Setup to-dos before build (David)
- [x] Google Cloud project + OAuth client + YouTube key — done, Supabase Google provider configured via `supabase config push` (`docs/setup-google-cloud.md`)
- [ ] CurseForge for Studios API key → `.env`
- [x] Supabase project **`odsens`** created (ref `dllbekulbimblrsrxuyv`, us-east-2) and linked from the repo (`supabase/config.toml`) — treated as **production**. Staging = **Supabase Branching** (decided; set up at first preview deploy) — **2026-08-20:** a persistent `staging` branch, not a branch per PR (ADR-0010)
- [x] Vercel project `odsens` linked (id `prj_fTdiX6oYxyQ8CnAmzSzKnCb74MkU`), Standard protection on, env vars seeded, `CRON_SECRET` generated
- [ ] Ko-fi account (18+, StudioBing) with page `oddsense` if available (phase 2)
- [x] Resend: Vercel integration installed (`RESEND_API_KEY` in all envs + local `.env`), domain verified (DKIM/SPF/MX), sender `allay@odsens.com`, test send OK
- [ ] **DMARC record** (Squarespace → Domains → odsens.com → DNS settings → Add record):
  - Type: `TXT` · Host/Name: `_dmarc` · Data/Value: `v=DMARC1; p=none; rua=mailto:david@studiobing.com; fo=1` · TTL: default
  - (`p=none` = monitor only; reports go to your inbox weekly. Tighten to `p=quarantine` later if desired.)
- [ ] **Inbound for allay@odsens.com** so replies to notifications don't bounce — pick one:
  - (a) *Squarespace email forwarding* (Domains → odsens.com → Email → Email forwarding): forward `allay@odsens.com` → `david@studiobing.com`. Squarespace will add its own MX records for `odsens.com` automatically (root MX only; the `send.odsens.com` MX for Resend is unaffected).
  - (b) *Resend Receiving*: Resend dashboard → Domains → odsens.com → enable receiving; add the MX record it shows (Type `MX`, Host `@`, Priority `10`, Value `inbound-smtp.<region>.amazonaws.com`) — then inbound mail arrives via Resend webhooks/dashboard.
  - Recommendation: **(a)** — simplest; you read replies in Gmail. Tell me when done and I'll set `Reply-To: allay@odsens.com` in templates.
- [ ] **Discord**: confirm Oliver has his own server; create a private `#odsens-admin` channel + webhook URL → `.env` `DISCORD_WEBHOOK_URL` (also settable in admin Settings)
- [x] Docker runtime on David's Mac — OrbStack installed, local Supabase verified (`docs/dev-tooling.md`)
- [ ] Decisions: Q33–40
- [x] Enable **Supabase Branching** on `odsens` + install the Supabase **GitHub integration** (`davidcbingham/odsens`) + the Supabase **Vercel integration** — REQUIRED before S1.1 (ADR-0006; at S0 the preview runs on placeholder anon/service-role values) — **done 2026-08-20:** Branching on, both integrations installed; previews run on the persistent `staging` branch (ADR-0010 supersedes ADR-0006; automatic per-PR branching is turned off)
- [ ] **Persistent `staging` branch — three dashboard steps (ADR-0010):** Supabase → GitHub integration → turn **off** automatic per-PR branching · Vercel → Preview environment → paste staging's `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY` (values never via chat) · Google Cloud console → OAuth client → add `https://oihrxwqarwllvsyllczo.supabase.co/auth/v1/callback` as an authorized redirect URI. Until all three are done a PR preview may still run on an ephemeral branch (`deploy-checker` reports which ref it found).
- [x] Vercel: enable **Protection Bypass for Automation** (Project → Deployment Protection) so `deploy-checker` can fetch protected previews — **done 2026-08-20:** enabled; `deploy-checker` sends the secret as the `x-vercel-protection-bypass` header (`.env.example` commented tooling line `VERCEL_AUTOMATION_BYPASS_SECRET` — documentation only, not a `lib/env.ts` / T-UNIT-35 name)
- [x] Vercel: remove `CURSEFORGE_MEMBER` from all environments (dropped from `.env.example` at S0, 04 SC-16) — **done 2026-08-20:** removed from every Vercel environment

## From "Seen on" + Workrooms (2026-08-17)
41. ~~**Mention click behaviour**~~ — **Answered (pass 3):** YouTube mentions embed inline (facade → player, indigo-lift outline, "on YouTube ↗" ghost link); other platforms link out.
42. ~~**Claude Design pass 3?**~~ — **Answered (pass 3):** Pass 3 done 2026-08-17 → `DESIGN.md` v1.3 §12. No pass 4 needed before build.
43. **Workroom email opt-in copy + privacy page line** — draft when Phase 2 starts.

## Design pass 3 — **landed 2026-08-17** (`design/claude-design-export/pass-3/`, `DESIGN.md` v1.3 §12)
Answers to Claude Design's open items (build decisions, no re-pass): view counts refresh hourly (data model) · general mentions tagged with the ODSENS wordmark chip · suggested-mention source = YouTube search per project title (v1.5) · one Discord webhook in v1 (a second mod-channel webhook is a field, not a redesign) · leaderboard linking = hashed-email match (decided Q33) · **nav shows "Commissions" only when Phase 2 ships**.
44. **Allay render** — Oliver to draw a pixel allay (Minecraft mob; same fan-content footing as the rest) for the email header (28px), Discord bot avatar, and Settings → Notifications. Also needed at build: official YouTube/TikTok/Twitch/Reddit marks (24px) from brand-guideline downloads. *(Oliver)*
45. **Workroom limits** — confirm 25 MB/file, 200 MB/room; **max participants per room** (proposed 6). *(Phase 2)*
46. **Future passes** — tell Claude Design to stop carrying pass-1/2 files forward (each pass re-adds ~12 MB of identical assets). Decide: self-contained snapshots (current) vs. delta-only exports.

## Engineering specs (2026-08-17)
`docs/build/00–05` + `_registry.md` + ADR-0001/0002 (with Amendment A) **FROZEN v1.0 (2026-08-17)** — build starts at S0 in a fresh session (`docs/build/START-BUILD.md`).

## S0 build notes (2026-08-17 → 2026-08-20)
- 2026-08-17 — S0 build started on `feat/S0-scaffold`; ADRs filed: ADR-0003 (CSP `unsafe-inline`), ADR-0004 (`/dev/components`), ADR-0005 (placeholder pages static per 02 RP-16), ADR-0006 (preview env fallback until Supabase Branching is live).
- ~~2026-08-17 — S1.1 open: preview `NEXT_PUBLIC_SITE_URL` strategy (per-branch env vs `VERCEL_BRANCH_URL` derivation) — needed for OAuth `redirectTo` on previews (ADR-0006 D2).~~ — **Answered 2026-08-20:** derived — on preview `NEXT_PUBLIC_SITE_URL = 'https://' + VERCEL_BRANCH_URL` (client: `NEXT_PUBLIC_VERCEL_BRANCH_URL`), no per-branch env step (ADR-0010).
- ~~2026-08-17 — S1.1 heads-up: Next 16 renamed `middleware.ts` → `proxy.ts`; 02 §3 / `_registry.md` say `middleware.ts` — decide by ADR at S1.1.~~ — **Answered 2026-08-20:** `proxy.ts` with the named export `proxy` (ADR-0009).
- 2026-08-20 — S0 session resumed after an interruption; PR opened from `feat/S0-scaffold`.
- 2026-08-20 — S0 gate notes (security-reviewer, spec-drift-reviewer; none blocking): (a) `lib/log.ts` now also redacts Google identity keys (`full_name`, `given_name`, `family_name`, `display_name`, `picture`, `avatar_url`, `phone`) and Discord webhook URLs without `token=` (01 INV-43) — S1.1 `/auth/callback` A3a is the first place a `user` object is in hand; (b) **S1.1 / supabase-ops + keep-docs:** 02 §4 and `supabase/config.toml` `[remotes.production.auth].additional_redirect_urls` allow `https://*.vercel.app/**` — any `*.vercel.app` site could start a PKCE flow against the project; narrow to `https://odsens-git-*-studiobing.vercel.app/**` + exact production hosts when S1.1 wires sign-in (ADR + 02 row) — **done 2026-08-20: ADR-0011**; (c) **upkeep:** GitHub Actions are pinned by major tag (`actions/checkout@v4`, `supabase/setup-cli@v1` + `version: latest`) — consider SHA pins + Dependabot; (d) `.env.example` / 04 SC-10 put a personal email in the Modrinth User-Agent contact — David to decide on a role address (spec change); (e) ADR-0003…0006 are dated 2026-08-17 (decision date) while PR #1 opened 2026-08-20 — left as decision dates; (f) C20 placeholder pages set `alternates.canonical` only and inherit the root OG image — real pages set per-page OG in their slices (02 RP-06).
- 2026-08-20 — **David to decide (design details DESIGN.md does not state; code follows 03 / the pass-3 prototypes for now — say "keep" or name the change, then `keep-docs` adds one DESIGN.md line each; a change = `Kind: design` ADR):** (1) footer links are `--chalk` 15px, hover `--white` (pass-3 frames show both this and `--indigo-lift` 14px); (2) the second action on the 404/error pages is a ghost button with → (03 G-02/G-03), pass-3 draws it as an outlined secondary; (3) 404/error pages render with no nav/footer (02 RP-09 root layout) — pass-3 frames show a slim wordmark bar; (4) phone menu: 15px `--mute` links, active = 3px gold underline, plain burger — pass-3 shows 16px `#C6CEDA`, a 4px gold left bar on the active row and an indigo-filled ✕; (5) the disabled/pending look (`--disabled-fill`/`--disabled-text`) applies to every Button variant, not only primary; (6) geometry not in DESIGN.md: toast `max-width: 360px`, 404/error column `max-width: 560px`, error line `max-width: 38ch`, error `!` glyph 40px (pass-3 34px), 404 title 40/30px (pass-3 26px).
- 2026-08-20 — S0 frontend-gate notes (none blocking): (a) `app/global-error.tsx` is the runtime root boundary, but Next's static `500.html` for hard server failures is its builtin page — `vercel-ops` forces a root error on the preview at S1.10 to confirm what visitors see; 03 G-03 says "inline minimal styles" and the file uses CSS Modules + globals (works; `keep-docs` may relax the wording); (b) the Supabase client chunk runs an `eval`/`Function` feature probe that the CSP blocks (benign, caught by the library) — known under ADR-0003; no CSP `report-to` until it is gone; (c) Lighthouse lab LCP on `/` is 3.0 s mobile-simulated with the footer line as LCP element — re-measure at S1.2 when the hero exists (❌ then if > 2.5 s); (d) **David:** there is no favicon (`/favicon.ico` 404s on every page) and DESIGN.md §10 is silent — OK to use the OddSense avatar (`assets/brand/avatar`) as `app/icon.png`? (design call; one DESIGN.md line).

## S1.1 build notes (2026-08-20)
- 2026-08-20 — S1.1 Accounts build started on `feat/S1.1-accounts`; ADRs filed: ADR-0009 (middleware file is `proxy.ts`), ADR-0010 (preview env from the persistent `staging` Supabase branch, `NEXT_PUBLIC_SITE_URL` derived from `VERCEL_BRANCH_URL`; supersedes ADR-0006), ADR-0011 (OAuth redirect allow-list narrowed to `https://odsens-git-*-studiobing.vercel.app/**`), ADR-0012 (`HASH_SECRET` boot-required — 9 names), ADR-0013 (`runAction` wrapper + `AuthError` export, `safeNext` in `lib/validation/next.ts`), ADR-0014 (`ProfilePanel` island on `/profile`, `handle_changed_at` in the own-row select, `AdminShell.viewer.avatarUrl?`, `GoogleSignInButton` `data-variant`), ADR-0015 (`profiles` mutations on other users' rows = service client only — an admin JWT cannot reach other rows, so 05 T-RLS-8/9 admin = D and T-RLS-5 admin = own row; schema as built), ADR-0016 (`/auth/callback` imports the admin client for the `email_hash` stamp — the one non-API `app/**` exception in 01 INV-14/INV-84; the INV-45/INV-46 greps exclude that file). Addenda 2026-08-20: ADR-0009 (`proxy.ts` imports `@supabase/ssr` directly — INV-13/INV-85 exception), ADR-0013 (`<actionName>Input` schemas live in `lib/actions/accounts.schema.ts` — Next 16 allows only async-function exports from a `'use server'` file), ADR-0014 (gate round 1, frontend #9: `ViewerProvider` publishes through a module-level external store + `useSyncExternalStore` instead of a React context, so a still-dehydrated `loading.tsx` boundary is never marked and `/profile` hydrates in place), ADR-0015 (gate round 1, security #16: `profiles.avatar_path` is constrained to the owner's folder by the CHECK `profiles_avatar_path_own` — migration `20260820120400_profiles_avatar_path_check.sql` — and every service-role Storage delete re-checks ownership via `lib/files.ts` `isOwnAvatarPath`; the `site_settings` singleton is created by migration `20260820120500_site_settings_default_row.sql`, not only by seed.sql).
- 2026-08-20 — 05 T-RLS-132 said the seed `moderation_mode` is `'hold_first_time'`; SEED-1, 00 S1.1 and 04 §1.3 seed `'auto'` and the test asserts `'auto'` — doc typo, cell corrected with a dated 05 §12 note (prose fix, no ADR).
- 2026-08-20 — `HASH_SECRET` set in Vercel **production + preview** and David's `.env` — **done 2026-08-20** (generated the same day; names only recorded here; ADR-0012).
- 2026-08-20 — local CI green (lint · unit 330 · db 367 · build + bundle-secret grep · e2e 56/56) before PR; zod removed from the client bundle (ADR-0008 D3 / ADR-0013)
47. ~~**Google sign-in on Supabase preview branches** *(David)* — each ephemeral preview branch has its own Auth callback (`https://<branch-ref>.supabase.co/auth/v1/callback`) that Google's OAuth client must list, and that list cannot be pushed from `config.toml`. On a preview, the sign-in *start* (the redirect to the branch's `/auth/v1/authorize?provider=google`) is verifiable; the Google consent leg is not unless Google knows that branch's callback. Options: (a) add one Google-console redirect URI per PR branch when a preview needs the full round-trip; (b) keep a persistent `staging` Supabase branch, register its callback once, and point `[remotes.staging]` at it; (c) verify the Google consent leg on production after merge only (Google already knows production's callback). **Foreman recommendation: (c), with (a) on demand for a PR that needs it; revisit at S1.10.** Nothing in code changes either way.~~ — **Answered 2026-08-20 (David):** option (b) — a persistent `staging` Supabase branch (git branch `staging`; `[remotes.staging]` in `supabase/config.toml`; its Auth callback registered once in the Google console) serves every preview, and automatic per-PR branching is turned off (ADR-0010).
- 2026-08-20 — **David to decide (design details DESIGN.md does not state; code follows the pass-3 frames for now — say "keep" or name the change, then `keep-docs` adds one DESIGN.md line each; a change = `Kind: design` ADR, D4 = `Kind: security` ADR; recorded by ADR-0014):** (D1) delete-account inline confirm copy "Delete your account? Your handle, picture and comments go with it." → "Delete it" / "Keep it" (follows the §11.2 comment-delete pattern) — keep?; (D2) avatar crop step copy "CROP IT SQUARE" + "Drag to move. Everything outside the box gets cut." (from the pass-3 frame) and the crop geometry 200px viewport / 150px gold box — keep?; (D3) the chalk "Continue with Google" button carries the pass-3 `4px 4px 0 --ink-deep` block shadow + hover lift like primary buttons (DESIGN.md §5 names only the chalk fill + dark text) — keep the shadow, or flat?; (D4) signing in from the admin gate lands on the home page (the RP-20 rule that blocks `/admin` as a return target); admins then use the profile menu's Admin item — OK, or should the gate return straight to `/admin`?; (D5) the profile page's "what we store" footer sentence and the picture-row hint ("Optional. Blank is a fine look.") — wording OK?
- 2026-08-20 — **S1.1 deploy-checker PASS on the preview** (staging branch wired, site URL derived, headers + bundle clean). Follow-ups for `vercel-ops` (David confirms each, one line): (a) `NOTIFY_FROM_EMAIL` exists twice in Vercel (a Preview-only row + a Prod/Prev/Dev row) — remove the duplicate; (b) the Supabase integration added production-only `POSTGRES_*`, `SUPABASE_JWT_SECRET`, `SUPABASE_SECRET_KEY` rows the app never reads — prune the sensitive ones (`POSTGRES_PASSWORD`, `SUPABASE_JWT_SECRET`) to shrink the blast radius; (c) `site_settings` has no row on the persistent `staging` branch (seed.sql runs only at branch creation) — a default-row migration (`insert … (id) values (1) on conflict do nothing`) lands in S1.1 so production + staging always have the singleton; seed.sql still sets `kofi_page`/`owner_profile_id` locally — **done 2026-08-20:** migration `20260820120500_site_settings_default_row.sql` (ADR-0015).
- 2026-08-20 — **Systemic gap (security-reviewer gate round 1 #16; fixed for avatars by the ADR-0015 addendum):** an owner-writable `*_path` column is a pointer into Storage, and a service-role delete that trusts it lets a user aim it at another user's object. Rule going forward: every owner-writable `*_path` column gets the same CHECK-to-owner pattern as `profiles_avatar_path_own` (the path must start with the owner's own id) plus an ownership re-check before any service-role Storage delete — `supabase-ops` applies it to skins / art at S1.7.
- 2026-08-20 — **Security gate round 2 (PASS) — open items, David to decide later:** (2) `images.remotePatterns` for the Supabase host has no `pathname` restriction (`/storage/v1/object/public/**`), so any path on that host can reach `/_next/image` — tightening is a 01 INV-54 edit → later ADR (`vercel-ops`); (3) 04 SC-05 scopes the banned check to insert actions, so a banned user can still rename (`updateProfile`) and change their picture — intended, or should bans freeze the handle too (ban-evasion by rename on existing comments)? A change = `Kind: security` ADR (`backend-robustness`) before S1.4.
- 2026-08-21 — **David on merge (S1.1):** banned accounts may delete themselves (follow-up PR right after S1.1: `deleteAccount` allowed for banned callers + a Delete account control on `/banned`; ADR-0019 follow-up) — **built 2026-08-27** (ADR-0021, `fix/S1.1-banned-delete`: `requireOnboarded({allowBanned:true})` passed only by `deleteAccount`; `BannedDelete` on the slab, onboarded banned only); look-alike handles (`0ddsense`, `admin1`) stay allowed for now (security round-3b advisory (5)). Follow-ups also logged from the last gate round: backend ⚠32 (structured detail on our own thrown errors in `runAction` logs), frontend ⚠14 (`app/icon.*` so `/favicon.ico` stops 404ing), schema row 20 (before the owner bootstrap run `select id, handle from public.profiles where handle is not null and public.is_reserved_handle(handle::text);` and free any squat).
- 2026-08-21 — **David's S1.1 preview review (decided; code + ADRs in PR #2):** (1) no Skip button on onboarding — leaving the picture empty and pressing DONE is the skip; DONE is gated on the handle only (ADR-0017); (2) after DONE the page now leaves with a document navigation — the old soft navigation reused a prefetched "go home → back to /welcome" answer from before the handle existed (ADR-0017), and the proxy no longer redirects Server Action POSTs — a second DONE used to be re-posted to `/` and crash the page (ADR-0009 addendum); (3) profile menu = Your profile · [Admin] · Sign out (ADR-0018); (4) deleting the account also signs the browser out so the header drops the name/picture at once (bug fix); (5) a banned user's Google sign-in lands on a banned page with nothing but Sign out (ADR-0019 — answers the security open item (3) above); (6) D1–D5: keep all. Oliver claims `oddsense` by the documented owner SQL (see the S1.1 notes below / `.claude/skills/supabase-ops/SKILL.md` "Owner bootstrap").
- 2026-08-20 — **Gate round 2 non-blocking follow-ups (owner `backend-robustness` / `test-engineer`; no ADR):** (a) `deleteAccount` deletes the avatar object before `auth.admin.deleteUser` — swap the order so a failed `deleteUser` cannot leave a dangling `avatar_path` behind the 1/day limit; (b) `proxy.ts` M4 ignores the select `error` — a PostgREST blip reads as a null handle and bounces onboarded users to `/welcome`; pass through (M8) on error; (c) `updateProfile` with handle + avatar in one call commits the rename before the upload (unreachable from `ProfilePanel`'s two forms); (d) no test exercises `deleteAvatarQuietly`'s two warn branches (`avatar_path_not_own`, `avatar_cleanup_failed`); (e) frontend ⚠: `AdminNav` prefetches the six admin routes that ship in S1.2+ (six RSC 404s on `/admin` load) — resolves as the routes land, 03 §2.10 kept as specified.
- 2026-08-21 — **David (S1.1 preview review):** the profile menu's "Change handle" / "Change picture" items were redundant with "Your profile" (all three open `/profile`) — the menu is now Your profile · [Admin — role ≥ moderator] · Sign out (ADR-0018, `Kind: design`; DESIGN.md v1.4 §11.1; 03 §2.5; 02 RP-12 + `/profile` row; T-E2E-32 updated). Bug fixed in the same round, no ADR (ADR-R9): deleting the account left the old handle + picture in the nav until a reload — `deleteAccount` clears the server cookies but the browser Supabase client kept its session; `ProfilePanel` now dispatches `odsens:viewer-signed-out` and `ViewerProvider` publishes `anon` + signs the browser client out locally (03 C-16a `ViewerProvider` row, prose).
- 2026-08-21 — **Security gate round 3 systemic gaps (fixed for H3 by ADR-0020):** (1) every rule implemented in an RPC/action must also bind the direct RLS write path (DB twin + T-RLS cell) — done for H3 by ADR-0020 (`is_reserved_handle()` + `profiles_guard`, migration `20260821090000_profiles_guard_reserved_and_banned.sql`; the browser holds the anon key + its own JWT and can PATCH its own row without any action); (2) `profiles_guard` is now ban-aware — a banned account's own-row write is 42501 (ADR-0020); (3) proxy M4b exempts `/api/*` — every future `/api/**` handler needs its own banned check (`backend-robustness`).
- 2026-08-21 — **S1.1 MERGED:** PR #2 squash-merged as `464d429` on `main`, tag `v0.2`; ADR-0009..0020 Accepted; gate round 3b all seven PASS. Production verified same day: Vercel Ready for `464d429`, `https://odsens.com` 308 → `https://www.odsens.com` 200, all 8 migrations applied on production Supabase by the GitHub integration, Google sign-in live. Note (docs corrected 2026-08-27): the production **custom domain is public** — Vercel Deployment Protection (Standard) guards `*.vercel.app` URLs and previews, not an attached custom domain, so the "behind Deployment Protection until S1.10" line was never true once odsens.com was attached; S1.10 keeps only the launch checks (DNS, Sentry, analytics, seeding). Still open from the merge: David's one real Google sign-in on production, then Oliver's first sign-in + the owner-bootstrap SQL (`supabase-ops` "Owner bootstrap"; run the squat-check SQL first).

## S1.2 build notes (2026-08-27)
- 2026-08-27 — S1.2 Projects (synced) Session-A build pass on `feat/S1.2-projects` (based on `fix/S1.1-banned-delete`; PR auto-retargets to main when #3 merges). ADR filed: ADR-0022 (`project_is_visible(uuid)` — the data-model §4 visibility predicate as one definer helper for the S1.2 SELECT policies; inline cross-referencing policies recurse 42P17). F-2 prose fixes: 04 `JobSummary.skipped` string|number (doc-internal reconciliation), 04 SC-07 `revalidateTag(tag,'max')` (Next 16.3 second argument), 03 §9 `--text-stat` 34px. Registry add-first additions: `lib/validation/filters.ts`, `lib/data/admin.ts`, `project_is_visible`.
- 2026-08-27 — **Spec tension for the gates (not resolved in code):** 02 §1.3 gives moderators admin-page read access, but 05's frozen T-RLS matrix denies moderators the hidden-project and `sync_runs` selects — the admin pages read through the cookie client under RLS, so a moderator sees published-only rows and no sync table (controls disabled either way; actions refuse server-side). If Session-B gates or David want full moderator visibility, that is an ADR + RLS change, not a page change.
- 2026-08-27 — **Real-catalogue finding (Session B staging sync, non-blocking):** the live Modrinth catalogue has one project (`loud-metal-pipe-mace`) with two versions sharing a `version_number`, so the second insert is refused by the DB unique `(project_id, version_number)` — the job records it per-item and the run stays `ok` (18/18 projects, one version row skipped), but the error will repeat on every sync including the first production one. Real fix needs a decision: widen the version identity (e.g. include channel), key strictly on `external_id`, or keep newest-wins. Owner: `backend-robustness`, next touch of the sync adapters (likely S1.3); ADR if the data-model unique changes.
- 2026-08-27 — `CURSEFORGE_API_KEY` still not obtained (docs/questions.md setup list): CF adapter/action/job ship verified against fixtures; `setProjectLink` answers `upstream_error` "CurseForge key not configured" until the key lands in Vercel + `.env` (AC8's CF leg then verifiable on the preview).
- 2026-08-27 — **S1.2 MERGED:** PR #3 (`fix/S1.1-banned-delete`, ADR-0021) squash-merged first, no tag; PR #4 squash-merged as `aeaf2b3` on `main`, tag `v0.3`; ADR-0021..0025 Accepted. All 12 gate verdicts PASS (PR #3 5/5 first pass; PR #4 spec-drift and design-fidelity after one fix round each — ADR-0023 CI build + local Supabase, ADR-0024 admin page server glue, ADR-0025 notFound interim, admin headers to Space Grotesk, breadcrumb links indigo-lift). Production verified same day: first prod Modrinth sync imported the live catalogue (18/18 projects; the known duplicate-`version_number` per-item error fired as predicted), `/projects` + detail pages live on www.odsens.com, headers (CSP · XFO DENY · HSTS) green, crons 401/405, `/nope-*` 404, unknown-slug 404 body + noindex per ADR-0025, `/dev/components` 404. Operational notes: the merge-to-main Vercel build raced production migration application (same shape as ADR-0023's staging note) and needed one `vercel redeploy`; deleting PR #3's branch closed PR #4 instead of retargeting (fixed by recreating the branch, reopening, retargeting to main, re-deleting). Owner bootstrap done earlier the same day on both envs (`oddsense` admin + `bizarobot` admin). Still open: CURSEFORGE_API_KEY; duplicate `version_number` decision (S1.3); Home hero/featured empty until Oliver/David curate on production.
- 2026-08-27 — **DECIDED (ADR-0025, Session B gate fix round): accept-and-monitor.** Unknown ISR slug routes serve the 404 body with status ∈ {200, 404} while the upstream Next bug stands; `app/not-found.tsx` gained `robots: { index: false }` (SEO mitigation); ISR cache growth is `SLUG_RE`-bounded and monitored, proxy-level allowlist recorded as the fallback; `upkeep` watches the Next issues and reverts the tolerance when fixed. Original entry follows for the option list: Next 16.3.1 upstream bug (vercel/next.js #45801, #76474): with any ancestor `loading.tsx` (which 02 RP-10 / 03 G-01 / T-E2E-18 require), the streamed shell commits status 200 before `notFound()` runs — mechanically incompatible with 02 §2.3 / SM-04 / 05 T-E2E-14/34's status-404 expectation. Interim: e2e asserts the 404 body and tolerates [200, 404] (loud spec-header comments). Side-effects for security-reviewer: each garbage slug writes a ~57KB ISR cache entry (unbounded growth from crawlers) and production serves 200s for unknown project URLs (SEO). Options when decided: drop the affected loading.tsx files (breaks RP-10/G-01), in-page Suspense workaround (same break), accept-and-monitor until the upstream fix, or a proxy-level slug allowlist check. Owner: `web-quality` + ADR.

## S1.3 build notes (2026-08-27)
- 2026-08-27 — S1.3 Exclusive projects Session-A build pass on `feat/S1.3-exclusive` (PR #5 docs close-out merged first so the branch bases on a clean main). ADR filed: ADR-0026 (version identity — `project_versions` unique moves to `external_id`, partial `(project_id, version_number) where external_id is null` for exclusives; `syncModrinth` matches by `external_id` only). This resolves the S1.2 duplicate-`version_number` finding above — the second-order behavior was worse than logged: on runs after the first failure the two upstream versions silently ping-ponged over one DB row (alternating overwrite + revalidation churn); keying on `external_id` ends both.
- 2026-08-27 — `supabase/config.toml` `file_size_limit` was already `100MiB` (raised with the S1.1 T-UNIT-18 read); no config change needed this slice (CI-13 satisfied).
- 2026-08-27 — Session-A verification sweep (5 adversarial agents, no blockers) → fix round + ADR-0027 (S1.3 contract clarifications): `setProjectLink` now refuses exclusive targets (`validation` — closes the AC8 gap where a CF link on an odsens row left the badge on cards while the detail page dropped it); `ExclusiveBadge` moved to `components/primitives/` (03 §2.2 home); `[id]` editor maps dotted zod issue paths onto their root `Field` like `/new`; `UploadWell` gained `disabledTitle` so an admin's not-ready file well never says "Admin only"; download route exports `OPTIONS` → 405; `uploadProjectFile` commit checks filename-uniqueness before the version-metadata upsert; sha512 line to `--text-label-sm`. Deferred to S1.7 with ADR-0027: the `Downloadable` counter-payload/URL-kind generalisation for kind `skin`. Harness learnings this slice: factory `id:` overrides are now the tracked id (leak fix); `.next` survives local rebuilds — stale `fetch-cache`/ISR output can poison the next build's prerender (CI is immune, fresh checkout; locally `rm -rf .next` before an e2e-truth build); Playwright's `admin` project is serial only within a FILE, so slice admin flows join `tests/e2e/admin/projects.spec.ts` instead of new files.
- 2026-08-27 — **S1.3 MERGED:** PR #6 squash-merged as `a21f443` on `main`, tag `v0.4`; ADR-0026/0027 flipped to Accepted in the merge. All eight gate verdicts on the PR PASS — six first-pass (spec-drift 16/16 · security 20/20 with the uploads/downloads deep-dive · backend · supabase · frontend with measured bundle deltas, max +5.0 KB gz on `/admin/projects/[id]` · deploy on the staging preview), design-fidelity after one fix round (`d5c657d`: exclusive-card body `padding-top: var(--space-40)` clears the badge; UploadWell uploading state `--indigo` → `--indigo-lift` (contrast 2.61/2.18 → 5.70/4.78, matches the pass-3 well); well zone de-rounded per §3; "Primary file" native checkbox → system `Toggle role="switch" accent="indigo"` (Comments-row idiom, e2e moved to `toggleFor()`); sha512 line 12 → 13px — all invented copy passed the voice check first-pass). Production verified same day: the merge build raced production migration application again (same shape as S1.2) — `supabase migration list --linked` until the three S1.3 migrations applied, then one `vercel redeploy`; `/` · `/projects` · detail 200, unknown slug 404 body (ADR-0025), headers green (CSP · XFO DENY · HSTS) with the `/api/download/**` `no-referrer` carve-out live; download route contract live (non-uuid and unknown uuid → 404 JSON, HEAD → 405, `no-store`/`nosniff`); crons: unauth 401, modrinth 200 with **errors: 0 — ADR-0026 confirmed against the real production duplicate** (the previously-failing version adopted this run: items 1 / versions 1), curseforge `skipped: not_configured`. Non-blocking gate follow-ups (owner `backend-robustness` unless noted, fold into the next S1.x robustness pass): (a) `updateExclusiveProject` with `{id}` and no optionals → empty PATCH `PGRST116` mapped to `internal` — short-circuit to `{id, slug}` (unreachable from the shipped editor); (b) download 500 log carries `meta:{name}` only — add a sanitized code so `record_download` vs signed-URL failures read apart in Vercel logs; (c) `20260827200000` bare `add constraint` isn't rerun-idempotent — pre-drop by name next time the family is touched (`supabase-ops`); (d) 02 §2.9 still carries the pre-A4 "rate-limit source" parenthetical — F-2 prose fix, twin of the data-model wording fixed in the PR (`keep-docs`). Operational notes: local e2e truth requires the `.env.test` build (`set -a; source .env.test; set +a; pnpm build`) — a plain `pnpm build` bakes `.env`'s staging Supabase into the bundle and 16 seed/auth specs fail against it; staging has no exclusive project (its data predates S1.3), so the staging badge/download demo needs one created via `/admin/projects/new`. Still open: CURSEFORGE_API_KEY; Home hero/featured empty until curated on production; production has no exclusive yet — Oliver's first real exclusive exercises create → upload → publish → download end-to-end; S1.7 `Downloadable` generalisation (ADR-0027 D5).

## S1.4 build notes (2026-09-03)
- 2026-09-03 — S1.4 Comments Session-A build pass on `feat/S1.4-comments` (off `main` 4ba8812, v0.4). Staging first: `staging` sat on the pre-squash S1.3 lineage (d8dad27), so the ADR-0010 fast-forward push could not land — realigned with a merge commit (`fe82d62`, tree identical to `main`) pushed fast-forward; the S1.4 branch merges into `staging` the same way before the preview. ADR filed: ADR-0028 (S1.4 contract clarifications — deleted-slot rule per 05 T-E2E-3, `reportComment` accepts reports on non-published rows per 04 §5.1 M8, the `comment_count`/`like_count` triggers vs `profiles_guard` (nested-trigger pass, migration `20260903090400`), schema helper/trigger names, `commentBodySchema` in the schema file, `ModActionRow surface`). The recorded 02-vs-05 moderator-reads tension needs NO ADR for this slice: `/admin/comments` reads only tables the frozen T-RLS matrix already grants to moderators (ADR-0028 D7).
- 2026-09-03 — Session-A build record: the subagent API was overloaded (HTTP 529) for about an hour, so the foreman built the schema (five migrations + SEED-9; a JWT smoke of the policies/triggers/RPC passed 44/44) and the whole server side solo; the parallel fan-out then delivered the public thread UI, `/admin/comments`, the db lane (54 files / 1319 tests), the unit lane (29 / 709) and the e2e lane (93 / 93 against the e2e-truth build). Findings folded into ADR-0028 D8–D11: a banned account never reaches a thread (proxy M4b sends it to `/banned` — 05 T-E2E-28/30 assert the redirect; the `CommentThread` banned shell stays for the mid-session case), the moderator merge resolves RPC authors through `public_profiles`, `renameUserHandleInput.handle` is a plain string so RPC `check_handle` can answer `handle_reserved`, and `stripHtml` drops `<script>` blocks whole (05 T-UNIT-5). Harness: `tests/helpers/globalSetup.db.ts` now waits for PostgREST's schema cache after `supabase db reset` (a full db run used to lose its first files to PGRST002); the Playwright `e2e` project runs after the smoke projects (the comment flows mutate the seed thread the smoke specs assert); `tests/helpers/threadCache.ts` repairs the ISR entry `next start` persists between local runs. Not a rule change: `next dev` appends a `nextjs-agent-rules` block to `CLAUDE.md` on every run (Next 16.3) — reverted, not committed; `upkeep` to decide.
- 2026-09-03 — Session-A verification sweep (5 adversarial lenses: contract, schema, security, UI, tests; 29 findings, none blocking, 5 major = 3 distinct defects) → fix round: (1) `comments_set_status()` now pins `created_at`/`updated_at` for JWT inserts — a direct PostgREST insert could forge a future `created_at` and hold an edit window that never closes; (2) `ReportPicker` value-imported the zod schema module (zod would have entered the `/projects/[slug]` bundle, ADR-0008 D3) — now a type-only import; (3) the T-E2E-4 comments-closed clause deferred at S1.3 is asserted. Foreman follow-through on the minors (ADR-0028 D12): column-level INSERT grants on the three comment tables, the reply-shape DB twin (`parent_id` must be a published root on the same target), the `edited_at` guard rule (it cannot move without a body edit nor precede `created_at`; the stamp itself stays the action's per 04 §1.2, so T-RLS-71's bare body PATCH still passes), a direct-insert backstop (60 / min · 600 / day per author — twelve times the 04 §5.5 ceilings), `banUserInput.banned` a strict boolean, `deleteComment` answers `banned` before any read, `CommentView.reportCount?` recorded (D9), the `check_handle`-as-moderator note (D10), plus UI polish (placeholder contrast to `--mute-dim`, `readOnly` instead of `disabled` while a composer submits, decorative avatars beside handles, the rename Field's structural reason as helper text, `data-reply` → a class). Noted, not changed: `comments_bump_comment_count()` counts an auto-held-then-approved comment twice and a held→hidden→unhidden one never — both benign for the first-timer rule (04 §1.2 letter kept); `CommentThread` receives `total` but recomputes it from the slot rule; a smoke failure now skips the flows (the `e2e` project depends on the smoke projects).
- 2026-09-03 — Local CI lessons (Session A): (1) 05 §6 COV-2 (85 lines / 80 branches on `lib/actions/**`, `app/api/**`, `app/auth/**`, enforced from S1.4) bit on the first measured run — `lib/actions/**` 75.3 % branches and `app/api/**` 61.1 % — mostly defensive `if (error) throw` arms and route error paths from S1.1–S1.3 code; a coverage-lift test pass closed it before the PR (tests only, thresholds untouched): `lib/actions/**` 91.3 % branches / 97.5 % lines, `app/api/**` 100 % / 98.7 %, via real DB races spliced into the actions' own sequences and per-call PostgREST fault injection (`tests/helpers/dbFault.ts`); dead defensive arms are listed, not deleted. (2) A row inserted through the service client does not reach the ISR page until the entry AND the tagged data-cache entry both regenerate — `revalidateTag(…, 'max')` is stale-while-revalidate at both layers, so one like/unlike pair can bake a stale thread for another 600 s; `tests/helpers/threadCache.ts` now cycles like/unlike until the fresh count serves, and the edit-window e2e runs first in the serial file (seed shape in, seed shape out). (3) An axe pass can catch a placeholder page before its stylesheet arrives (one smoke-phone flake on `/art`; CI's single retry covers it, the spec passes on re-run).

- 2026-09-03 — Session-A close-out (PR #8): the first CI run failed only the db lane, on 05 T-RLS-134's "`service_role` not granted" clause for `moderator_thread`. Cause: the newer `supabase/postgres` image CI pulls (`setup-cli@v1` `latest`) carries a default ACL for role `postgres` in schema `public` that grants EXECUTE on every new function to `anon`, `authenticated` and `service_role`; the local image (CLI 2.114.0) has that default only for `supabase_admin`, so locally the migration's `revoke … from public, anon` left the intended ACL and in CI it left `service_role` with EXECUTE. Fix = migration `20260903090500_moderator_thread_revoke_service_role.sql` (explicit `revoke … from public, anon, service_role` + the single `authenticated` grant — idempotent, no behaviour change: the server reads `moderator_thread` with the cookie client only). Lesson for every future function: name every role in the revoke — `revoke all … from public, anon, authenticated, service_role` — then grant the intended set; `check_handle` / `can_comment` carry the same latent image-dependent `service_role` EXECUTE (unasserted, harmless) and get the same treatment in the next slice that touches them.

- 2026-09-03 — Session-B gate round 1 (PR #8): `backend-reviewer` ✅ (advisories: emit-after-write retry duplicate, `moderateComment` not compare-and-set, queue capped at 300 newest rows — recorded below as follow-ups), `security-reviewer` ✅ (non-blocking: like/report INSERT policies do not bind the target comment's own status; no DB transition table for moderator status writes; `notification_events.payload` keeps `{profile_id, handle}` after `deleteAccount` — retention to decide before S1.5 wires recipients), `deploy-checker` ✅ on the preview, `spec-drift-reviewer` ❌ ×2, both doc-side (code unchanged): (1) 03 §2.4 / 00 §S1.4 said the optimistic insert is skipped only for *first-timers* under `hold_first_time`, but the client cannot tell a first-timer apart (C-17a exposes no `comment_count`) — `CommentThread` inserts optimistically only under `auto`; recorded as ADR-0028 D13 and the two docs amended; (2) 01 INV-66 and the 01 module list still placed `commentBodySchema` in `lib/validation/comment.ts` after ADR-0028 D5 moved it — INV-66, the module list and 01's Status line amended, ADR-0028 "Docs amended" gains the rows. Also fixed: the `next.config.ts` `agentRules: false` comment claimed a questions.md note that did not exist — this is it: Next 16.3 `next dev` upserts a managed `nextjs-agent-rules` block into `CLAUDE.md` whenever it detects a coding agent; the flag disables that rewrite (dev-tooling only, no runtime effect; not an `experimental.*` key). `deploy-checker` correction to the Session-A record: the persistent `staging` Supabase branch does NOT carry SEED-9 — Branching runs `seed.sql` only on branch creation/reset, and `staging` predates the file — so the preview's threads start at `0 TOTAL` (empty state renders correctly); the migrations `20260903090000..090500` are applied. Pre-existing cosmetic: `/admin/<sub>` pages title `<Page> · Admin` while rendering the anon gate (the page `metadata` wins over the layout) — 02 §3 wording or a layout `generateMetadata`, later slice.
- 2026-09-03 — S1.4 gate follow-ups (non-blocking, owner `backend-robustness` unless noted): (a) `postComment`/`reportComment`/`moderateComment` return `internal` when `emit()` fails after the row was written — a retry could post a duplicate; catch + log the emit failure and still answer `ok`, or record as the accepted trade-off. (b) `moderateComment`'s status UPDATE is not compare-and-set (`.eq('status', row.status)` → empty → `conflict`, the auto-hold pattern) — two moderators racing can re-publish a deleted comment. (c) `listModerationQueue` reads the 300 newest non-deleted rows before grouping, so a held comment older than 300 newer ones drops out of the queue while the sidebar count still counts it; read `status='held'` unconditionally, and cap the per-target `moderator_thread` fan-out. (d) `supabase-ops`: `comment_likes`/`comment_reports` INSERT policies bind `can_comment(target)` but not the comment's own status (a direct insert can like/report an own held/hidden row — counter on a non-public row, actions refuse it); `check_handle` gets the every-role revoke + a `service_role` denied assertion in the next slice touching it. (e) `renameUserHandleInput.handle` error message says "20 characters maximum" while the schema allows 64 — cosmetic. (f) `frontend-reviewer` ✅: fixed in the round — the admin queue's Project link measured 70×38, now a 44px inline-flex target (03 C-24). Follow-ups: `test-engineer` — 00 S1.4.AC17's "composer focused / inline confirm open" axe runs are not in `tests/e2e/flows/comments.spec.ts` (verified clean by hand, 16 scans, 0 serious/critical); `web-quality`/`vercel-ops` — lab LCP ≈ 4.4 s on `next start` is the site-wide render-blocking-CSS signature (known-open #3), not an S1.4 regression; measure production RUM after the merge. `design-fidelity-reviewer` ✅, UNSPECIFIED for `DESIGN.md` later: Approve hover `--emerald-soft`, the "Replying to @handle" label word, comment handle text in `--white` (the pass-3 frame's `#fff`). `supabase-reviewer` ✅ (db reset + 1417 tests, six-migration re-run idempotent, types byte-identical); follow-up for the next migration touching these tables: indexes on the four uncovered FKs `comments.moderated_by`, `comment_reports.reporter_id`, `comment_reports.resolved_by`, `notification_events.actor_id` (deleteAccount/service cascade paths only); cosmetic: the definer counter writes also bump `updated_at` on the row they touch. Round 2 (`b1200c0`): `spec-drift-reviewer` ❌ ×3, all the same two doc sweeps reaching further — D13 wording also in 04 §1.2 `postComment` Effects and data-model §2.5; D5's schema home also in 01 §1 "Module ownership" prose and 05 T-UNIT-4; the 06 README index row missed `§2.4 CommentThread` — fixed as round 2, no code change
- 2026-09-03 — **S1.4 MERGED:** PR #8 squash-merged as `0c92ef0` on `main`, tag `v0.5`; ADR-0028 (D1–D13) flipped to Accepted in the merge (`04c2d38`). All seven gate verdicts on the PR PASS — six first-pass (backend · security · supabase with a full `db reset` + 1417 tests and a six-migration idempotent re-run · design-fidelity with 16 screenshots against the pass-3 frames · frontend with the route table diffed against `main`'s CI artifact, S1.4 delta ≈ 9–13 KB gz on `/projects/[slug]` · deploy on the staging preview), spec-drift after two doc-only rounds (`b1200c0`, `8836849`: ADR-0028 D13 "optimistic insert only under `auto`" + the D5 `commentBodySchema` home swept through 00/01/03/04/05/data-model/README; no code changed except the 44 px admin-queue link). David's preview walkthrough → "merge". Production: the merge build (`odsens-qlgy8hvzb`) errored on prerender — `Could not find the table 'public.comments_public'` — because Vercel built before the Supabase GitHub integration applied the six migrations (the S1.3 race, third time: S1.2, S1.3, S1.4); production kept serving v0.4 meanwhile; `supabase migration list --linked` reached 6/6 about ten minutes after the merge, then one `vercel redeploy` (`odsens-faxis49qi`, Ready, aliased to www.odsens.com). Verified same day: `/` 200; a live project detail renders COMMENTS · `0 TOTAL` · the sign-in prompt, no composer for anon; `/admin/comments` anon → 200 `AdminGate` ("ADMINS ONLY"); cron unauth 401; CSP · HSTS · XFO DENY live; `deploy-checker` production checklist PASS (17/17): deployment `dpl_FPPo4oMzg9ZGSKYsuTeHWQQTUzV3` = `0c92ef0`, 22/22 migrations local = remote, all 9 boot env names + optionals present, only the production Supabase ref in the 17 chunks (staging ref absent), secret grep clean, crons registered (unauth 401; `sync-modrinth` authorized once → 200, items 2 / versions 2 / errors 0 — `sync_runs` shape and behaviour unchanged), headers green, titles right, `/how-comments-work` 200, unknown slug → the ADR-0025 interim 200 + 404 shell, `/admin/comments` anon gate with `noindex` + `no-store`, sign-in start reaches Google (not completed). Lesson to make structural before S1.5: the ISR build reads the new schema at prerender time, so **every slice that adds a table/view the public pages read will hit this race** — options for `vercel-ops`/`supabase-ops`: (a) make the build tolerant (`lib/data/*` reads return empty on `PGRST205` schema-cache misses during `next build` only), (b) hold the Vercel production build until the integration reports the migrations applied (Vercel "Ignored Build Step" polling `supabase migration list`), or (c) accept the one manual `vercel redeploy` per slice as the documented ritual (`ship` skill). Decide in S1.5's Session A..

## S1.5 build notes (2026-09-03)
- 2026-09-03 — S1.5 Notifications build started on `feat/S1.5-notifications`; ADRs filed: ADR-0029 (`build-waits-for-schema`, `Kind: addition` — the prerender-vs-migration race decision: `pnpm build` runs `scripts/wait-for-schema.mjs` before `next build`, polling PostgREST until every repo migration is applied (RPC `migration_versions()`, service_role only) and every created relation is in the schema cache (OpenAPI `definitions`); 20 min deadline on Vercel, 60 s elsewhere; the manual `vercel redeploy` stays the fallback in `ship` step 10 — options (a) build-tolerant reads and (b) the Vercel Ignored Build Step were rejected, reasons in the ADR), ADR-0030 (`s15-contract-clarifications`, `Kind: deviation` — D1 shared job runner with J-F edge emission, D2 `runNotify` orchestrator, D3 `sync.stale` subject = `sync_source`/deterministic uuid + never-run sources never stale + `mentions` off until S1.8, **D4 retention decision: `notification_events` rows survive `deleteAccount`, every `{profile_id, handle}` payload reference to the deleted user is scrubbed to `{null, null}` before `deleteUser`** (closes the S1.4 security follow-up), D5 `NotificationMatrix` owns the whole Settings form + ADR-0024 glue for the Moderators rows, D6 adapters over `fetchJson` (no `resend` SDK), D7 `Digest` template, D8 fixture-server POST routes, D9 Discord `retry_after` as ms per the frozen 04 §4.6 — **follow-up (`backend-robustness`): Discord's live 429 body carries `retry_after` in seconds; decide whether 04 §4.6 changes**, D10 matrix seeded by its migration, D11 the S1.4 follow-ups folded in (`check_handle`/`can_comment` every-role revoke; the four FK indexes), D12 `maskSecret` home, D13 no `Reply-To` until allay@ inbound forwarding is confirmed). `@react-email/components` (INV-78) is flagged deprecated on npm — its sub-packages are current; `upkeep` may move to `react-email`'s runtime exports with an INV-78 ADR later.
- 2026-09-03 — S1.5 Session-A build pass (7 lanes) done; foreman calls recorded in ADR-0030 D14–D19. Invented copy for the design gate (voice-checked, prototype-verbatim where the pass-3 frames had words): settings page — see ADR-0030 D19; emails/embeds — CommentReported sign-off "— the allay. It just carries the report. The call is yours.", meta "<n> report(s) · <reasons>", Digest lead "The allay picked up <N> things. Here they are:" + overflow "…and <N> more in admin." + sign-off "— the allay. It waited until there was a pile. Here's the pile.", SyncFailed h1 "<Source> sync" + meta "Last run <when>." / "Last good run <when>." / "No good run yet.", excerpt attribution "— <handle>" / "— a deleted account", badge words NEW COMMENT · HELD FOR REVIEW · REPORTED · FAILED · STALE · DIGEST, buttons View comment · Approve · Review · See sync status · Open admin, footer switch words comment mail · held-comment mail · reported-comment mail · sync mail · digest mail; Discord embed descriptions `@handle: "…"` / `First comment from @handle: "…"` / `<n> reports · <reason>`; setUserRole lines "You can't change your own role." / "Someone has to stay admin."; testDiscordWebhook "Add a webhook URL first." / "Discord didn't answer."; zod lines in `lib/actions/settings.schema.ts` ("That doesn't look like a Discord webhook URL." …). Permanent constant: `SYNC_SOURCE_NAMESPACE = 7f2b6c3e-8a41-5d09-9b7e-2c4f1a6d3e58` (stored `sync.stale` subject_ids are v5 UUIDs under it). Harness: the local DB is shared — never run `supabase db reset` while another db/e2e run is in flight (a parallel session's resets produced phantom failures during the build pass).
- 2026-09-03 — S1.5 Session-A verify+fix round (7 lenses → 5 fixers, then the foreman's tail): 0 blockers, every major fixed — `setUserRole` refuses a banned target for a staff role (`conflict` "Unban that account first.", ADR-0030 D14); J-F reads the previous FINALIZED run (`ok IS NOT NULL`, D1, `tests/db/jobs/runner.test.ts`); delivery is at-least-once with per-unit marking and a tick-anchored time budget (04 N2/N4); Discord descriptions are markdown-escaped and links are stripped from every excerpt at the source so mail and embed read the same words (04 N6 / 03 E-05); subjects are clipped to ≤ 60 code points (03 E-06); "No good run yet." (mail) / "<Source> counts haven't updated yet." (Discord) replace the word "never" (D19); the Discord sync embed title is `Sync failed — Modrinth` (bare source, pass-3 frame); `scripts/check-fixtures.mjs` exempts retina asset names (05 F-3); `Toggle` gains `describedBy` (03 C-03). New copy for the design gate: "Unban that account first." · "That doesn't look like a Discord webhook URL." · "<Source> counts haven't updated yet." Follow-ups, none blocking: (1) upstream dedupe for a re-sent unit — a Resend `Idempotency-Key: <row.id>:<attempts>` (a bare row id is unsafe: Resend rejects a reused key whose payload differs, and the rendered body changes between ticks); (2) `lib/env.ts` could drop the test-only `*_API_BASE` / `E2E` names when `VERCEL_ENV` is set (ADR-0002 #73 addendum), or `deploy-checker` asserts they are absent from Vercel; (3) a failed `runNotify` tick emits `sync.failed {source:'notify'}` through the shared runner (04 §3 "every job") — a "Sync failed: notify" mail under the default matrix; excluding `notify` from J-F is a gate decision; (4) optional column-level `grant update (enabled) on notification_matrix to authenticated` (data-model §4 wording) + a T-RLS-100 sub-case; (5) `PixelLabel` has no fill+tone combination rule, so COMING LATER is a plain mute-dim label (D19) — add the rule if the design gate wants the pass-3 tag look.
- 2026-09-03 — S1.5 Session-B gate round 1 (PR #10): `supabase-reviewer` ✅ (follow-up: FK index on `notification_recipients.profile_id` when Phase 2 fills it), `backend-reviewer` ✅ (advisories: `DELIVER_TIME_BUDGET_MS` 40 s + one ~47 s worst-case SC-09 send overran the route's 60 s `maxDuration` → tuned to 12 000 in this round (ADR-0030 D20d); per-row 23505 fallback in `notifyFanOut` can leave an event with one channel row after a non-23505 write fault — edge, open; Discord `retry_after` units + Resend `Idempotency-Key` already listed), `design-fidelity-reviewer` ✅ 32/32 (DESIGN.md-unspecified, recorded not fixed: COMING LATER rows show disabled toggles at seeded values vs the pass-3 empty squares; admin-email chips sit above a separate add field; phone Test button content-width vs full-width Add; mixed `’`/`'` in the island copy; Discord "View" is the embed title url per 04 N6), `frontend-reviewer` ✅ (axe `landmark-unique` on the MODERATORS section vs the `Table` region → caption "Moderators table", this round; Lighthouse desktop 100/100/96, LCP 0.7 s, CLS 0; pre-existing: `/projects` grid prerenders as skeletons under the `useSearchParams` bailout — wrap in `<Suspense>` if SEO/no-JS matters), `security-reviewer` ✅ 18/18 (advisories: `.max(16)` on `updateSettings.matrix`; `SKIP_SCHEMA_WAIT` has no `VERCEL_ENV` guard and test-only `*_API_BASE` are honoured by production code paths — same as follow-up (2) above; `/admin/settings?error=` reflects React-escaped text), `deploy-checker` ✅ 19/19 on the preview (build log `wait-for-schema: ready (27 migrations, 20 relations)` — ADR-0029 works on a preview build; one authorized `notify` ping → 200 JobSummary), `spec-drift-reviewer` ❌×3, all doc-side → ADR-0030 D20 (02 §1.3 + 05 T-E2E-35 name the "New exclusive project" entry button; 01 INV-78 lists `@react-email/ui`; 01 §1 tree names `runner.ts`/`notify.ts`/`deliver/{content,types}.ts`). Harness: the frontend gate's Lighthouse run coincided with the local Docker (OrbStack) daemon stopping — `supabase status` fails until it is restarted.
- 2026-09-06 — **Production bug fixed (config only, no code, no ADR): sign-out returned 403 `{"ok":false,"error":{"code":"forbidden","message":"Nope."}}`.** Vercel had `www.odsens.com` as the primary production domain with `odsens.com` 308 → `www` (set when the domain was attached in S1.1), while `NEXT_PUBLIC_SITE_URL` in the Production env is `https://odsens.com`. `app/auth/sign-out/route.ts` compares the request `Origin` host against the `NEXT_PUBLIC_SITE_URL` host (04 §2.2 CSRF rule), so a browser posting the sign-out form from `www.odsens.com` failed the check and rendered the JSON 403 as a white page. Sign-in was unaffected because the OAuth callback is a GET that Vercel's 308 forwards intact; a POST cannot survive the same hop. **No test could catch it:** T-ACT-9 builds the request `Origin` *from* `NEXT_PUBLIC_SITE_URL`, e2e runs on `localhost:3000`, and previews derive `NEXT_PUBLIC_SITE_URL` from `VERCEL_BRANCH_URL` (ADR-0010) — production was the only environment where the served host differed from the configured one. **Fix:** swapped the Vercel project domains via the REST API — cleared `redirect` on `odsens.com` first (so the two could never point at each other), then set `www.odsens.com` → `redirect: odsens.com`, `redirectStatusCode: 308`. The apex was always the intended canonical: `supabase/config.toml` `site_url`, 01 INV-34, the `<link rel="canonical">` tags, `app/sitemap.ts`, `app/robots.ts`, the S1.5 email links and 00 S1.10.AC1 ("`https://odsens.com` and `https://www.odsens.com` (redirect)") all say apex — the Vercel domain setting was the lone outlier, so no spec changed. Verified live: apex 200 on `/`, `/projects`, `/privacy`, `/how-comments-work`; `www` 308 → apex preserving path + query; no redirect loop; `POST /auth/sign-out` with `Origin: https://odsens.com` → 303 → `https://odsens.com/`. Side effects: sessions were host-only cookies on `www`, so everyone signed in before the swap must sign in once more; a stale `www` tab submitting sign-out still 403s once (the 308 re-POSTs carrying the `www` Origin) until reloaded; canonical/sitemap/email links now resolve directly instead of through a redirect. Docs corrected: `docs/dev-tooling.md`, `.claude/skills/ship/SKILL.md` (×2), `.claude/skills/vercel-ops/SKILL.md`, `docs/build/START-BUILD.md` — historical S1.1–S1.4 merge records keep their `www` wording as the record of what was true then. **Follow-up DONE the same day — ADR-0031 (`sign-out-csrf-request-host`, `Kind: deviation`):** `app/auth/sign-out/route.ts` no longer depends on the Vercel domain and `NEXT_PUBLIC_SITE_URL` agreeing. The CSRF guard accepts the `Origin`/`Referer` host when it equals `request.nextUrl.host` (the host the request was addressed to — the OWASP same-origin check) **or** the configured `NEXT_PUBLIC_SITE_URL` host (kept, so the frozen comparison stays true as a subset and every existing T-ACT-9 row passes unchanged), and the 303 now targets `new URL('/', request.nextUrl)` so logout returns the user to the host they were actually on. Not a weakening: both accepted hosts are hosts the deployment answers on, and a cross-site form cannot set `Origin` (CSP `form-action 'self'` remains the second layer, 01 INV-77). T-ACT-9 gains the two rows that would have caught the live bug — own-host `Origin` ≠ `NEXT_PUBLIC_SITE_URL` → 303 with `Location` on that host, and a foreign `Origin` on that same non-configured host still 403 with the session untouched. Docs amended: 04 §2.2 + §7 env matrix, 02 §4 Sign-out + §1.2 route table row, 05 T-ACT-9, three Status lines, the 06 README index.
- 2026-09-06 — **S1.3 entry point fixed and shipped ahead of S1.5 (no ADR): `/admin/projects` never linked to `/admin/projects/new`.** David found it on production — the create form, `createExclusiveProject`, the two-phase uploads and T-E2E-35 all shipped in S1.3 (2026-08-27) and work, but the list page (S1.2, built before the create page existed) never got the entry point 02 §1.3 names in its `/admin/projects/new` row (`Projects → "New exclusive project"`); T-E2E-35 navigated straight to the URL, so nothing failed. Fixed on `fix/S1.3-admin-new-project-button` (PR #11 → `main`; CI green, `spec-drift-reviewer` PASS 15/15): a `secondary size="sm"` `Button` in the ALL PROJECTS heading row, rendered the same for both admin roles (navigation, not a mutation — 03 §2.10 governs mutation controls, and the create page keeps its rendered-disabled treatment for moderators), plus T-E2E-35 now clicking through from the list instead of `goto`. No ADR: the frozen spec already required the button, so its absence was the drift. Split out of PR #10 rather than waiting on S1.5 — see ADR-0030 D20(a).
- 2026-09-06 — **Brand: the crown alone is the site mark (ADR-0032, `DESIGN.md` v1.6).** Oliver asked for just the crown as the website logo rather than his personal OddSense avatar — the crown is the website, the full character is him, used as his own account picture. David replaced `assets/brand/avatar/oddsense-avatar-5000.png` with crown-only art and regenerated the 80/112/160 derivatives (commit `74b508a`); `DESIGN.md` §2 and §10 reworded, palette tokens unchanged. **Open for David:** all three repo usages of `/brand/avatar-80.png` share that one file, so the nav (right by the decision) *and* the two "OddSense makes things for Minecraft" intro strips (Home, featured hero — `alt="OddSense"`, person-context) now render the crown. Keeping the character in those two needs a second asset + a code change; taking the crown everywhere needs nothing. → ADR-0032 decision 4.
- 2026-09-06 — **David: "The Admin section of the site has no button to get back to the main site."** True since S1.1 — every sidebar item is `/admin/*`, the ADMIN label links to `/admin`, and `ProfileMenu` (Your profile · Admin · Sign out, ADR-0018) never reaches `/`, so a moderator had to edit the URL to see the live site. **Fixed by ADR-0033 (`admin-header-site-link`, `Kind: deviation`):** the `AdminShell` header strip now opens with the odsens mark — `Avatar size=28` of the crown (ADR-0032) + the `ODSENS` wordmark, one `<a href="/">` with `aria-label="odsens home"` — then the ADMIN label (still `<a href="/admin">`, `aria-label="Admin home"`), then `ProfileMenu` right; the two left links share one flex group so the strip stays a two-way split. It mirrors `Nav` N-02 at admin scale: same composition and tokens, avatar 28 rather than 40 because the strip's other elements are a 12px `PixelLabel` and the 28px menu trigger. No new component, no new token. Rejected: a `ProfileMenu` item (ADR-0018 deliberately cut that menu to three), a sidebar item (02 RP-14 pins that order and it is the admin section's own map), repointing the ADMIN label at `/` (loses the dashboard link and reads as a trap). Verified at 1280 and 375 px — no overflow (header 375, `documentElement.scrollWidth` 375). Docs amended: 03 §2.10 + Status line, `DESIGN.md` §9 (9) + changelog v1.7, 05 T-E2E-33 + Status line, the 06 README index. Follow-up for `design-fidelity`: the pass-2/pass-3 admin frames still show the strip without the mark and are stale until the export is regenerated.
- 2026-09-11 — **S1.5 MERGED:** PR #10 squash-merged as `1e981ef` on `main`, tag `v0.6`; ADR-0029 (build waits for schema) + ADR-0030 (D1–D20) flipped to Accepted in the merge (`d6ccdb6`), together with the three cross-cutting ADRs that rode the branch from 2026-09-06 — ADR-0031 (sign-out CSRF request host), ADR-0032 (crown is the site mark, `DESIGN.md` v1.6), ADR-0033 (admin header odsens mark → `/`, `DESIGN.md` v1.7). Merge order: PR #11 (the S1.3 "New exclusive project" button) first as `b209df1` (no tag, production `odsens-nn3hltyuw` clean), then `main` merged back into the branch (`dbc5e54`; one comment-only conflict in `app/admin/projects/page.tsx`). All seven gate verdicts on the PR PASS (round 1 on `3805424`, spec-drift round 2 on `4c0ddcc`); David's 2026-09-06 fixes had run the full local chain green. CI on the merged tree failed ONCE, e2e only: T-E2E-34 admin (`tests/e2e/admin/projects.spec.ts:240`, fourteen navigations with ISR re-checks) timed out twice inside `submitAndWait` — it had taken 27.9 s on the previous green run against the FLK-4 30 s default, so it was a latent budget flake, not a regression (the tree was docs-only different from the run that passed). Fix `0b2cf8a`/`1858deb`: `test.setTimeout(90_000)` on that one test, the precedent T-E2E-24 already uses in `flows/comments.spec.ts`; no app code changed, no ADR (harness config, FLK-4's default stands). Production: the merge build (`odsens-853rkueuc`) was ADR-0029's first real pass — `wait-for-schema: waiting (missing migrations: …)` then `wait-for-schema: ready (27 migrations, 20 relations)` 45 s later, `Build Completed` at 1 min, no redeploy needed; `supabase migration list --linked` shows all five `20260903120000..120400` applied. Verified on `https://odsens.com`: `/`, `/projects` (20 cards in headless Chromium — the grid is a client island, so `curl` sees only the streamed shell), `/projects/disabilities`, `/support`, sitemap + robots 200; `/admin` and `/admin/settings` unauthenticated → the root 404 body (streamed 200, ADR-0025); CSP / `X-Frame-Options: DENY` / HSTS present; cron routes 401 without the secret, with it `notify` ok (fan_out + deliver, 0 items, `sync_runs` rows every 5 min), `sync-curseforge` `skipped: not_configured`, `sync-modrinth` ok on the S1.5 shared runner (items 1; the hourly 15:07 / 16:07 / 17:07 runs were ok too); zero browser console errors. `staging` realigned with `main` (`ff01696`, take main on conflicts; tree-identical). **Build paused here by David's call (2026-09-11): before S1.6 opens, Oliver's directly-reported bugs and spec misses (not yet written down anywhere in the repo) get triaged with David and folded into the plan — either as fix PRs off `main` or as reprioritised slices; that triage decides what S1.6 Session A actually is.**

## Oliver's first-use list (2026-09-11) — triage + fix passes
- 2026-09-11 — **Oliver's issues, relayed by David (not previously written down anywhere), with the disposition agreed with David the same day.** (1) "fabric" → "Fabric" → **fix pass 1** (ADR-0034 D2, display names only; ids stay data). (2) Modrinth-synced images over-compressed → **fix pass 1** (ADR-0034 D4: it was not compression — Modrinth's API hands out its 96 px icon and 350 px gallery thumbnails and the site upscaled them; the originals are now stored and `next/image` quality 90 on the big slots). (3) "Download directly from site, not from Modrinth" → part of the **cross-posting slice** below (odsens-hosted file = the primary download whenever one exists). (4) Device-style dropdowns → **fix pass 2**: every `Select` becomes the themed listbox (David: all dropdowns, admin included). (5) Thick white avatar outline → **fix pass 2**: 3 px → 2 px, the card/button outline width, on every avatar and the crown mark (DESIGN.md rule change → ADR). (6) "Essential Dark Pack Fix" 404 → **fix pass 1** (ADR-0034 D1: Modrinth slug `essential-dark-pack-armor-fix-` has a trailing dash, `SLUG_RE` refuses it; slugs are now normalised at sync). (7) Version lists → ranges → **fix pass 1** (ADR-0034 D3, versions-table Minecraft column). (8) Subtle page animations when switching sections → **fix pass 2**: a 120–180 ms fade on route change, `prefers-reduced-motion` respected (DESIGN.md §8 → ADR). (9) Donation slogans ("Keeps the mods free and the pipe loud.") → **fix pass 2**: one plain line, "Support OddSense on Ko-fi" (David). (10) Ko-fi → David entered `odsens` in `/admin/settings` (S1.5) the same day; the public Support page + floating button are **pulled forward from S1.9 ahead of S1.6** (David: yes) → ADR + 00 plan edit as its own slice. (11) Comma-separated loaders on the create/edit forms → **fix pass 2**: a fixed-list checkbox grid for loaders; game versions stay typed. (12) "The site does not allow uploading projects" — the biggest miss: S1.3 shipped hosted uploads, but the entry point to `/admin/projects/new` was missing until PR #11 (2026-09-11) and the two-step shape (create the draft first, then upload files) was not obvious; David is checking with Oliver whether anything else is missing. What Oliver actually needs is below.
- 2026-09-11 — **Cross-posting decision (David, for a new slice ahead of Videos; ADR + 00 plan edit to follow):** Oliver posts a project to odsens.com the moment it is ready and submits the same project to Modrinth in parallel; Modrinth's review takes long or never approves. Model agreed: **one project, many homes.** The odsens row is canonical (URL, hosted files, curation stay); the admin editor gets a "Modrinth listing" field beside the CurseForge one (paste a URL) and the sync suggests a match by slug/title for Oliver to confirm; a linked Modrinth project never creates a second row — its versions sync **into** the same project keyed by Modrinth version id (ADR-0026 already lets exclusive and synced versions coexist), files paired by sha512 when Oliver uploaded the same file to both; **the odsens-hosted file is always the primary DOWNLOAD** (Oliver will keep uploading new versions to both, and may back-fill hosted versions on older Modrinth-only projects), with "Also on Modrinth / CurseForge" rows + the combined count underneath; **the ONLY ON ODSENS badge simply disappears once the project is on both**; the same mechanism serves Modrinth and CurseForge; uploads open to every project (so a Modrinth-first project can gain odsens-hosted files and download from the site). Sequence agreed: fix pass 1 → fix pass 2 → cross-posting slice → Support page slice → then S1.6 Videos.
- 2026-09-11 — **Fix pass 1 (ADR-0034, branch `fix/oliver-pass-1-catalogue`, no slice):** D1 `normalizeSyncedSlug` in `mapProject` (`slugify` → title → `p-<id>`; the production row heals on the next hourly sync because the job already revalidates a renamed slug's old and new paths); D2 `lib/format/loader.ts` `loaderLabel` applied in the read model (`projectChips`, `versions[].loaders`, `primaryFile.loaders`); D3 `lib/versions.ts` `formatVersionList` in the `VersionsTable` Minecraft cell (runs of neighbouring minors → `first – last`, break on a skipped minor series, snapshots verbatim — no upstream version list needed); D4 gallery `raw_url ?? url`, `createModrinth.resolveIconUrl` (HEAD-probe `.png|.jpg|.jpeg|.webp|.gif` behind a resized `…_96.webp`, only when `iconBase` changed), `images.qualities [75, 90]` + `quality={90}` on detail icon / hero / gallery / lightbox. Tests T-UNIT-47/48, T-ADP-21; fixture icons are plain `.png` so db/e2e never probe. Local Docker was down — db/e2e ran in CI only.
- 2026-09-11 — **Fix pass 2 (ADR-0035, branch `fix/oliver-pass-2-look`, no slice):** D1 `Select` rebuilt as the themed listbox (APG select-only combobox on a `<button role="combobox">` named by the label, `role="listbox"` panel in the profile-menu recipe, keyboard + type-ahead + click-outside, hidden `<input name>` so server forms are unchanged) and moved onto the 03 §1.4 client-island list — every dropdown, admin included (David); D2 avatar outline 3 px → 2 px everywhere, `Avatar` `border` prop removed; D3 `app/(public)/template.tsx` + `app/admin/template.tsx` route fade (`--dur-fast`, 3 px lift, opacity-only under reduced motion — DESIGN.md §8 v1.8); D4 new `CheckGrid` primitive for Loaders on `/admin/projects/new`, the `[id]` DETAILS form and the `UploadWell` version form (options `LOADER_OPTIONS`, glue reads `formData.getAll`); D5 tip line "Support OddSense on Ko-fi." Tests: T-E2E-2 listbox leg, T-E2E-35 combobox/grid steps, T-E2E-50 fade + outline. Local Docker still down — e2e in CI only.
- 2026-09-11 — **Build order (ADR-0036):** two slices inserted between S1.5 and S1.6 — **S1.5a Cross-posted projects** (`v0.6.1`, the "one project, many homes" model above) and **S1.5b Support page** (`v0.6.2`, the public half of S1.9: `/support`, `FloatingSupportButton`, `tip_click`, Ko-fi CSP); S1.9 narrowed to Stats + `sign_in`; the registry's slice-ID format gains the inserted form `S<phase>.<n><letter>`; `START-BUILD.md` now names the current position (it had said S0 since day one). S1.5a's contracts (link actions, adoption/fold rules, `Downloadable` for synced rows, `is_exclusive`) are pinned by its own ADR at Session A.

## S1.5a build notes (2026-09-11)
- 2026-09-11 — **S1.5a Cross-posted projects contract (ADR-0037, `Kind: deviation`, branch `feat/S1.5a-cross-posting`).** The ADR pins what ADR-0036 left to the slice: one pair of link actions, `linkProjectListing` / `unlinkProjectListing` (`platform: modrinth | curseforge`; `setProjectLink` retired — an exclusive may now gain a CurseForge link and loses its badge by design); the sync resolves a linked listing into its canonical odsens row and never touches that row's metadata (versions adopt by `version_number`, files pair by sha512, a hosted icon wins on any row); a listing the sync had already imported is folded by RPC `fold_project` (service-role only, one transaction — the one recorded exception to "never delete synced rows") and the old slug keeps resolving through a `project_redirects` row; uploads and publish work on every `source`; the GET IT primary is the hosted primary file of the newest version that has one, else the Modrinth page; `projects_public.is_exclusive` (odsens + no link) drives the badge everywhere.
- 2026-09-11 — **Pre-build adversarial review of the first draft (three lenses) changed the letter before any code:** the link row is written *before* the fold so a failed fold converges on the next sync; `project_versions_external_id_key` is non-deferrable, so the merge deletes the duplicate version row before the canonical row takes its id; the primary cross-posting case (Oliver's odsens `foo` exists when Modrinth approves `foo`) no longer fails the hourly sync — the listing lands as `p-<id>` and the `/admin/projects` note matches on the title too; adoption applies to Modrinth-first rows as well; counters on a folded row are carried (they are not re-derivable); the redirect runs from `generateMetadata` as well as the page (it runs first); replacing a Modrinth listing is `conflict` "Remove the current Modrinth listing first." so the un-adopt always runs.
- 2026-09-11 — **Session A (ultracode ON)** built the slice in parallel lanes on `feat/S1.5a-cross-posting` — four migrations (listing unique, `project_redirects`, `is_exclusive` on the view, `fold_project`), adapter `getProject` / `parseModrinthRef`, the editor's "Modrinth listing" field, the `/admin/projects` match note, `hostedFirst` / `selectPrimaryFile`, the fixture-server `.json` fallback — with 05 §8 row S1.5a assigned (T-RLS-135..137, T-ACT-79..84, T-ADP-22, T-UNIT-49..50, T-E2E-51..53) and the spec amendments in the same PR; the PR opens at CI green and Session B (ultracode OFF) runs the gates. Product decision recorded above under "Cross-posting decision" — no `docs/spec.md` change.
- 2026-09-11 — **Session B (ultracode OFF) — gates on PR #16.** Seven gates: spec-drift ❌ (doc-only: the dots-only `parseModrinthRef` clause, T-E2E-34 in the amended list, `routeFade.ts` in 05 §1.3, one stale action name in the security agent prompt — all fixed in the PR), design-fidelity ❌ (the listing grid broke at 640px; now the DESIGN.md §6 tablet floor 600px), supabase / backend / security / frontend / deploy ✅. Advisories recorded here, **not in this PR**: (1) `fold_project` has three preconditions (rows exist and differ, duplicate is `modrinth`, canonical is `odsens`); it does not itself check that the duplicate's listing is linked to the canonical — the action guarantees it (the link row is written first) and the RPC is service-role only, so the narrow J-D exception rests on `linkProjectListing`. A fourth precondition means re-stating the whole function in a new migration; do it when the RPC next changes (`supabase-ops`). (2) CurseForge `project_links.url` is stored verbatim from the API's `links.websiteUrl` (`z.string()`, S1.2); the Modrinth side derives its URL from the id — tighten the CurseForge schema to `https://www.curseforge.com/…` or build the URL from the slug (`backend-robustness`, small). (3) Order-B sha512 pairing (Modrinth file synced first → hosted upload → next sync) is the same row state T-ACT-82's rerun case already asserts (hosted `storage_path` + `url`, `files === 0`); no separate case added. (4) Design, unspecified: a synced row's read-only Modrinth field looks like an editable one (DESIGN.md §5 has no read-only `Field` state), and the linked Modrinth field echoes the URL while the CurseForge field echoes the bare id — Oliver/David to rule (default proposed: leave both as built; a read-only state joins DESIGN.md if it ever bothers Oliver). (5) `lib/actions/run.ts` collapses every zod message to "Check the form." (S1.2 convention) so the plain-words Modrinth ref message never reaches the field — consider surfacing `issues[0].message` in a later pass (`web-quality`). (6) Synced Markdown bodies inject author headings (`##`, `####`) under ABOUT — a heading-order axe *moderate*; a `Markdown` demotion is a later `web-quality` item. (7) `.claude/skills/vercel-ops/SKILL.md` says Preview carries the new `PUBLISHABLE_KEY`/`SECRET_KEY` names; the project has the legacy pair in Preview (both ADR-0010-valid) — fix the sentence in the close-out docs PR. (8) Staging held a pre-ADR-0034 slug `essential-dark-pack-armor-fix-`; the gate's authorized sync rewrote it — production converges on its first post-merge cron run, no action.
- 2026-09-12 — **S1.5a MERGED:** PR #16 squash-merged as `4706631` on `main`, tag `v0.6.1`; ADR-0037 (D1–D11) flipped to Accepted on the branch (`22f9c18`) before the merge. Gates: round 1 on `6f7f6eb` — spec-drift ❌ (four doc-only rows), design-fidelity ❌ (a 640px breakpoint), supabase / backend / security / frontend / deploy ✅; one fix round `a70dda4` (docs, the 600px tablet floor, nbsp in the match note); both re-runs ✅ — nine verdicts in the PR body. Advisories deferred (the Session B note above): the fold RPC's fourth precondition, CurseForge `url` validation, a read-only `Field` look, the zod-message collapse, Markdown heading demotion. David reviewed the preview (`odsens-c68ko6j88`) and said merge. Production: the merge build (`odsens-dnumhxtmt`) waited on ADR-0029 — `wait-for-schema: waiting (missing migrations: 20260911120000..120300; missing relations: project_redirects)` for ~90 s, then `ready (31 migrations, 21 relations)`, `Build Completed` at 2 min, no redeploy; `supabase migration list --linked` shows all four `20260911120000..120300` applied. Verified on `https://odsens.com`: `/projects` 200, `/projects/loud-metal-pipe-mace` 200 with the GET IT panel, `/admin/projects` 200 (the `AdminGate` shell unauthenticated); cron `sync-modrinth` 401 without the secret, `ok` with it on the shared runner (items 0 — nothing changed upstream); `projects_public.is_exclusive` live — `phobias` is production's one unlinked exclusive and shows ONLY ON ODSENS on its detail page and the home hero (the `/projects` grid is a client island; its RSC payload carries `exclusive:true`); `project_redirects` reads `[]` for anon. `staging` realigned with `main` (`0db5c07`, take main on the two ADR-status conflicts; tree-identical). **Next: S1.5b — Support page** (`v0.6.2`, 00 §S1.5b, ADR-0036 D3).


## Oliver's second-use list (2026-09-12) — triage + fix pass 3
- 2026-09-12 — **Oliver's punch list, relayed by David, with the disposition agreed the same day.** (1) Usage analytics in admin (visit count, where people came from) → **Vercel Web Analytics first** (David: dashboard first; an admin page later once we know what Oliver looks at) — fix pass 3 mounts `<Analytics />` on the public routes (ADR-0038 D4; was S1.10); David toggles Web Analytics on in the Vercel project. (2) No "project saved" signal when editing or creating → **"Saved." toast** after every admin save, "Project created." after create (ADR-0038 D1). (3) No way to delete a gallery image → **Delete for uploaded images (object removed), Hide/Show for Modrinth images** (the sync would bring a deleted one back; David) (ADR-0038 D3). (4) Gallery preview → **thumbnails in the editor** (David: not a public lightbox) (D3). (5) Image names → **Modrinth's name shown and editable here; uploaded images get a name box** (David) (D3). (6) Section sidebar in the editor (General / Description / Gallery / Versions…) → **one section at a time**, with an unsaved-changes dot on the section title and a modal when leaving a section with unsaved edits (David) → **deferred to its own slice** (ADR-0038 D6): a redesign plan with documentation and a 00 build-plan insertion before S1.5b, so Oliver can keep adding content while the rest of the plan proceeds. (7) Markdown toolbar on the description (H1–H3, bold, italic, strike, code, lists, link, image, YouTube) with Preview → **description + notes, with Preview** (David) → same deferred slice. (8) The crown next to ODSENS should have no white box → **the crown site mark is unframed** in the Nav and the Admin header (ADR-0038 D2, DESIGN.md v1.9).
- 2026-09-12 — **Fix pass 3 (ADR-0038, branch `fix/oliver-pass-3-editor-feedback`, no slice):** D1 `SavedToast` island + `?saved=` on the admin PRG round-trips; D2 `Avatar.frame='none'` on the Nav / Admin crown; D3 `project_overrides.gallery_overrides` (migration `20260912120000`), `curateProject.gallery_overrides`, `updateExclusiveProject.gallery`, dropped uploads delete their Storage object, `mergeGallery` applies the overrides, the editor's GALLERY section (thumbnail · Name · Hide/Show or Delete · Save names) on both branches, `Button.formAction`; D4 `@vercel/analytics` mounted; D5 T-UNIT-51, T-ACT-85/86, T-E2E-54, T-E2E-50 amended; D6 the editor redesign deferred to its own slice.
- 2026-09-12 — **Fix pass 3 MERGED:** PR #19 squash-merged as `7c492a6` on `main`, no tag (fix pass); ADR-0038 flipped to Accepted (`aec10ac`). Gates: round 1 on `73ef68b` — spec-drift ❌ (two doc rows), backend ❌ (a truncated gallery post could delete pictures by omission), frontend ❌ (Enter in a Name field fired the first row's Delete/Hide), supabase / security / design-fidelity ✅; fix round `42ff446` (hidden default submit, no-truncate posts, upload cap, https-only urls, extras keep description) + `2cbcd4a` (odsens-side cap, one 04 wording); re-runs ✅; deploy ✅ on the `2cbcd4a` preview. David reviewed the preview, enabled Web Analytics on the Vercel project and said merge. Production: migration `20260912120000` applied (`gallery_overrides` reads `[]` through the anon REST), the merge build live — `/`, `/projects`, `/admin/projects` 200, the Nav crown carries `data-frame="none"`, `/_vercel/insights/script.js` served, cron `sync-modrinth` ok. `staging` realigned with `main` (`d4608b8`, take main on the ADR-status conflict; tree-identical). Advisories left open: the db-lane harness races Storage right after `supabase db reset` (a `waitForStorage()` before `uploadSeedObjects()` would close it — `test-engineer`); the home hero / detail icon LCP image is lazy (no `priority`) — pre-existing, a small `web-quality` follow-up.
- 2026-09-12 — **Editor redesign planned as S1.5c — Project editor v2 (ADR-0039), inserted BEFORE S1.5b Support (tag `v0.6.2`; Support moves to `v0.6.3`).** Items 6 + 7 of the list, with David's additions: one section at a time behind a sidebar (`?section=`), an unsaved-changes dot on the active section and a Stay / Leave anyway dialog when leaving with unsaved edits (plus the browser prompt on close), and a Markdown toolbar (H1–H3, bold, italic, strike, code, lists, quote, link, image, YouTube) with a Preview that renders through the site's own renderer on both the description and the notes fields. No server contract or schema changes. DESIGN.md v1.10 is written in the slice's Session A unless Oliver runs a Claude Design pass first (`docs/design-process.md`).
- 2026-09-12 — **S1.5c Session A (build pass, branch `feat/S1.5c-project-editor-v2`, PR #21):** the editor shows one section at a time behind `?section=` (General · Description · Gallery · Versions · Listings · Publish — a synced project has no Publish; unknown → General; every save lands back on its section with the "Saved." toast); `EditorSections` island (the sidebar / phone chip row, the unsaved dot + sr "Unsaved changes", the browser prompt, and the Stay / Leave anyway `Dialog` on any same-document link click while dirty — the admin nav included; remounted per section); `MarkdownEditor` island on Body (odsens) and Notes (synced) with the 13-button toolbar, Ctrl/Cmd+B/I, roving focus and a Preview rendered by the site's own `renderMarkdown()` (`lib/markdown.ts` drops `server-only` and reads `publicEnv`); DESIGN.md v1.10 §11.3 #20 + the `--scrim` token; ADR-0040 (Proposed) records the Session A findings (publicEnv, six `Icon` glyphs, the document-level link guard, the INV-86 wording, moderator Preview view-only, per-section remount); tests T-UNIT-52/53 and T-E2E-55..57 assigned, T-E2E-35/42/51/53/54 amended in place (`?section=`); `scripts/check-test-ids.mjs` enforces S1.5c from this PR.
- 2026-09-17 — **S1.5c Session B (gates on PR #21).** Round 1 on `99c1da2`: supabase / backend / security / deploy PASS; spec-drift ❌ (03 C-04 class names in `EditorSections`; ADR-0040's "Docs amended" table short of three edits), design-fidelity ❌ (at 390 the visually-hidden "Unsaved changes" label resolved against the viewport and widened the page while a late section was unsaved), frontend ❌ (the toolbar's 44px targets overlapped in-group — the second ❌ on that item, so it went to David). **DECIDED (David, 2026-09-17 — ADR-0040 D12):** the toolbar buttons sit 8px apart with a full 44×44 target on touch (<900px or `pointer: coarse`); with a mouse at ≥900px the designed 4px pitch stays and in-group neighbours split the gap (40×44) — the one accepted exception to 03 C-24, admin-only. Also built in the fix rounds (`8ca0543`, `2e2b4e8`, and the round-3 commit): the chip is the sr label's containing block; the active chip is scrolled into view on mount (ADR-0040 D11; DESIGN.md v1.10 §11.3 #20); the last chip carries 8px end padding so its focus ring is never cut (frontend round 3 ❌); no `title="Admin only"` wrapper around `MarkdownEditor` (D5); an eslint fence for `@/lib/markdown` (01 INV-86 described a rule `server-only` had stood in for); `mute` / `mute-dim` on `slab-sunk` and `gold` on `gold-wash` gated in `scripts/contrast.mjs`; on touch the toolbar separator's push-back margin is 0 so wrapped rows align. A reviewer-suggested per-form submit re-base was built, tested and backed out: every submit's PRG re-renders the section from the server (observed on the production build, even when the redirect URL is unchanged), so its premise was false. A gate agent's probe saved 65,536 characters into the seed exclusive's `body_md` on the LOCAL stack; David approved a `supabase db reset` (nothing outside the machine was touched) — gate prompts now say "no form submits".
- 2026-09-17 — **S1.5c follow-ups (none built; next fix pass unless noted).** Editor guard: the browser's Back button while unsaved leaves without asking; submitting one form drops an edit typed in another form of the same section (Listings has two); the text of a REFUSED save is not echoed back (ADR-0040 Consequences). Editor a11y / perf: put the section in the page `<title>` so the route announcer speaks on a section switch, and move focus to the section heading; lazy-load the Preview renderer (`next/dynamic`, ~50 kB gz on five sections that never use it); `app/admin/projects/[id]/loading.module.css` still draws the pre-v2 single column; `listAdminProjectVersions` runs on every section; the Preview pane's `aria-live` is inert; the admin shell prefetches four routes that do not exist yet (pre-existing). Server glue: `saveBody` / `saveNotes` treat an ABSENT field like an empty one (harden like the gallery "never delete by omission" guard); `saveCommentsEnabled` ignores the action result and always toasts "Saved." (pre-existing); `?error=` text is echoed into the alert line — round-trip a code instead (pre-existing); CRLF counts twice against the `body_md` / `notes_md` caps (needs an ADR — 04 contract); anon `<title>` on an editor URL names a published project because `generateMetadata` runs before the gate (pre-existing; return `{}` when there is no viewer). Tests / CI: pin the trickier `javascript:` / `data:` / host-confusion vectors in T-UNIT-14 now that the renderer ships to a client bundle; no CI types-drift check (`supabase gen types` diff); one 1024px editor shot; a T-E2E-57 clause that the moderator's Preview switch has no `[title="Admin only"]` ancestor (ADR-0040 D5 has no regression lock). Editor polish from the last gate round: at 390, Tab onto a chip that is only partly inside the row does not reveal it (run the D11 alignment from a `focusin` on the nav); the unsaved dot widens the active chip by 16px after D11 aligned it, so on `?section=listings` the chip's right border is cut by ~8px (effect deps `[active]` → `[active, dirty]`); the word "Preview" sits outside the Toggle's `<label>`, so clicking the word does nothing; the three S1.5c rows in 03 §2.10 have 7 cells under a 9-column header. Public site, pre-existing: the `FilterBar` type row at 390 probably clips the focus ring of its first and last chip the same way the editor's chip row did. **Design questions for DESIGN.md §11.3 #20 (Oliver / David):** whether the Preview control may take its own row on touch (it does: the 8px-pitch toolbar is 594px wide); the toolbar button `:active` look; the toolbar look while Preview is ON (built: all 13 disabled at 45%); the text-glyph size (built: 13px); the `pre` surface inside the `--slab-sunk` Preview well (it disappears); 00 / 03 / ADR-0039 say "§9 Admin" where DESIGN.md's reference is §6 #9 (03 fixed here; ADR-0039 is Accepted and stays).
- 2026-09-18 — **S1.5c MERGED:** PR #21 squash-merged as `3becef1` on `main`, tag `v0.6.2`; ADR-0040 (D1–D12) flipped to Accepted on the branch (`f4c698e`) before the merge. Gates: round 1 on `99c1da2` — supabase / backend / security / deploy PASS, spec-drift ❌, design-fidelity ❌, frontend ❌ (second ❌ on the toolbar target → David's decision, D12); frontend round 3 on `2e2b4e8` ❌ (one new item, the last chip's focus ring); final on `43cac0f` — spec-drift, design-fidelity, frontend, deploy PASS (backend / security / supabase stand from `99c1da2`: the delta touched no server code, schema or contract). CI green on the merge head (lint, unit 2818, db 1813, build, e2e). Session B was paused overnight when David closed the machine (three gate re-runs died and were re-spawned fresh); the four round-1 PASS verdicts live as PR comments, all seven final verdicts are in the PR body. **Production verified 2026-09-18:** the merge build (`3becef1`) logged `wait-for-schema: ready (32 migrations, 21 relations)` and went Ready; `odsens.com` `/`, `/projects`, `/projects/pixel-chameleon`, `/support`, `/admin` → 200 (`/admin` = the gate; an editor URL as anon returns the gate only — no editor markup), CSP / `X-Frame-Options: DENY` / HSTS / nosniff present, `www` → 308 apex, the three cron routes → 401 without the secret (authorized ping not run — `CRON_SECRET` is not on this machine). `staging` realigned to `main` (`0f75b15`, content-identical). `.claude/agents/frontend-reviewer.md` now says its probes never submit forms or write to the local database.

## S1.5b build notes (2026-09-18)
- 2026-09-18 — **S1.5b Session A (build pass, branch `feat/S1.5b-support-page`).** `/support` is real: title + "Everything here is free. This is just if you feel like it.", the gold `AmountPicker` ($1 / $3 / $5 / Other, $3 preselected, CONTINUE ON KO-FI mounts Ko-fi's panel in place, "on Ko-fi ↗" leaves the site), "What it pays for", the SUPPORTERS board in its empty state. `FloatingSupportButton` on every public page but `/support`; `TipPanel` (detail rail + new Home compact row) and the button fire `tip_click`. No schema, action or CSP change. Findings → **ADR-0041** (Proposed): the Ko-fi `frame-src` entry stays site-wide — a `/support`-only header would block the panel after a client-side navigation — and "only on `/support`" is the INV-58 code rule; `AmountPicker` owns the loaded flag; `TrackedLink` gained `'gold-ink'` + `aria-label`; "Tips open soon." is gold-ink on gold. Tests: T-E2E-11 (smoke at 1280 + 390, plus the Settings → `/support` leg in the admin spec), T-E2E-49, T-UNIT-54.
- 2026-09-18 — **Where the Ko-fi page name lives:** Admin → Settings → Ko-fi → "Page name". Empty = `/support` shows "Tips open soon." with the picker disabled; saving a name opens tips within the same minute (`revalidateTag('settings')`). Production value not checked from this machine (`.env` is the local stack).
- 2026-09-18 — **Open after S1.5b:** Ko-fi's preset-amount URL parameter is still unverified (04 §5.7, ADR-0002 #50) — the chosen amount is used for the analytics event only; check once Oliver's page takes a real tip *(closed the same day: there is no such parameter — "Support page after first use" below, ADR-0042)*. The S1.5c follow-ups above remain their own fix pass.
- 2026-09-18 — **S1.5b MERGED:** PR #23 squash-merged as `285fab6` on `main`, tag `v0.6.3`; ADR-0041 (D1–D8) flipped to Accepted on the branch (`c686fb6`) before the merge. Gates: round 1 on `531a19a` — supabase / backend / security / deploy PASS; spec-drift ❌ (ADR-0041's decisions had not reached 00 S1.9.AC4, the 01 INV-58 Check cell, the 05 T-E2E-49 cell and 02 §2.1 States — docs only); design-fidelity ❌ (the Other field's focus ring was gold on gold because `Field` draws its ring on the well; a 16px phone inset on the floating button); frontend ❌ (the same ring; no `app/(public)/support/loading.tsx`). Fix round `7894be7` → spec-drift and design-fidelity PASS; frontend ❌ on two NEW items, both layout shift on `/support` at phone width (the new shell's footer jump; the actions row re-wrapping on font swap at 390px). `878ce11` → frontend ❌ on one more new item (the same re-wrap in the tips-closed state), design-fidelity spot-check PASS, deploy PASS again. `5353a3b` → frontend PASS (CLS 0.0004 at 390; it was 0.10–0.12). No item failed twice, so nothing went to David. Every verdict is a PR comment; the seven final `GATE:` lines are in the PR body. CI green on the merge head (lint, unit 2822, db 1813, build, e2e). Session B was paused once when David closed the machine (the frontend re-run died and was re-spawned); the pause note lived in Claude's memory. David tried the preview and said merge. **Production verified** on `https://odsens.com` (deployment built from `285fab6`, `wait-for-schema: ready (32 migrations, 21 relations)`): `/support` 200 in the **open** state — $3 preselected, nothing disabled, "on Ko-fi ↗" → `https://ko-fi.com/odsens`, so the live Ko-fi page name is set; no iframe and no Ko-fi script before a click; the floating button and the compact `TipPanel` on `/`, the full `TipPanel` on a project page, no button on `/support` or `/admin`; CSP byte-identical on `/` and `/support` with the unchanged `frame-src`; `/support` prerendered at 10 min; client chunks clean; crons 401 without the secret; `www` 308s to the apex. `staging` realigned with `main` (`7b3d6fe`, tree-identical). The preview showed "Tips open soon." throughout because the staging database has no Ko-fi page name. **Next: S1.6 — Videos** (00 §S1.6; Session A also moves Latest videos + Find me beside the Home compact panel — ADR-0041 D6).
- 2026-09-18 — **S1.5b follow-ups (none built; a fix pass of their own, beside the S1.5c follow-ups — none blocks S1.6).** Ko-fi: ~~verify the preset-amount URL parameter~~ (done 2026-09-18 — there is none; ADR-0042 removed the picker, see the next section); test an iframe `sandbox` token set (`allow-scripts allow-same-origin allow-forms allow-popups allow-popups-to-escape-sandbox`, probably `allow-top-navigation-by-user-activation`) against a real tip — adopt it and write it into 01 INV-58 if nothing breaks (security gate). `lib/support.ts`: have `normalizeKofiPage` return `null` unless the value matches `KOFI_PAGE_RE`, and add `.` / `..` to T-UNIT-54 — today `.`/`..` collapse to Ko-fi's home page, reachable only by a direct DB write (`site_settings.kofi_page` has no CHECK; the origin cannot be escaped). ~~`.env.example` seeds `KOFI_PAGE=oddsense`~~ (now `odsens` — ADR-0042). Fonts: tune the `next/font/local` fallback metrics (`adjustFontFallback` / `size-adjust`) so the fallback's advance width matches Space Grotesk — that retires font-swap re-wraps at the root: a small copy re-wrap on `/support` at 360 and 430 (CLS ≈0.02, within budget) and a **pre-existing** header re-wrap on `/projects/[slug]` at 390 (CLS 0.109 on `metal-pipe-mace`, on `main` before this slice); the Nav wordmark is the LCP element on every page (simulated slow-4G LCP 4.1–4.4 s, unchanged by this slice) — measure on production, where Lighthouse is not behind the preview login. Ops: the authorized cron 200 cannot be checked from a local `.env` (it holds the local-stack secret) — confirm from Vercel's cron logs or keep a tooling-only copy; `NOTIFY_FROM_EMAIL` still exists twice on Vercel (since S1.1); `.claude/skills/vercel-ops/SKILL.md` lists the sync-youtube and stats-snapshot crons as if they were live. **Design questions for DESIGN.md (Oliver / David):** ~~a hover look for the amount chips; the disabled look for the chips~~ (moot — the chips went with ADR-0042); the colour of a ghost link on a gold slab (built: `--gold-ink` underlined — the standard `--indigo-lift` is 1.96:1 on gold); the heart glyph size in the 52px square (built 19px, prototype 20px); the 16px phone gutter on the text pages (`/support`, `/privacy`, `/how-comments-work`) where §3 says 24; whether a skeleton for an accent slab shows the accent fill while loading (built: the `/support` card shell is a dark slab, the loaded one is gold — geometry matches, only the colour changes).

## Support page after first use (2026-09-18)
- 2026-09-18 — **David used the live Support page the day it merged:** he picked $1, pressed CONTINUE, and Ko-fi's panel opened underneath saying $5 — two boxes, the second contradicting the first. He asked (1) whether Ko-fi has an API for a tighter integration and, if not, (2) for the Ko-fi panel to replace the gold card rather than sit under it.
- 2026-09-18 — **What Ko-fi offers (checked against Oliver's live panel in a real browser, plus Ko-fi's own docs pasted by David):** no payment API — the only developer surface is a **webhook** that reports a tip after it happens (that is S2.1's supporters board; the payload carries the tipper's email, so only its keyed hash is stored, and `is_public: false` hides the message); the **iframe panel** we already embed; and an **overlay script** (`overlay-widget.js`, a floating button that opens the same panel in a pop-up) which we do not load — third-party code on every page before anyone asks, a looser script policy, and only the button's colour and text are ours. The panel **cannot be handed an amount**: it opened at $5 with every URL parameter tried, and its page reads neither the URL nor messages from the host page. This closes the "verify the preset-amount parameter" item carried since the design review (#13) and ADR-0002 #50.
- 2026-09-18 — **DECIDED (David): "A + (a)", built before S1.6 as a fix pass (ADR-0042, branch `fix/support-one-button`).** (A) the $1 / $3 / $5 / Other picker goes — the gold card keeps its title, one line and a single TIP ON KO-FI button, and pressing it **replaces the card with Ko-fi's panel** (a small bar above it has "← Back" and the "on Ko-fi ↗" link); the amount is chosen once, in Ko-fi. (a) the floating ♥ button stays a link to the Support page. **Parked for David + Oliver:** (b) the floating button opening our own pop-up in place — possible without Ko-fi's script, since the frame is already allowed site-wide, but it rewrites the "Ko-fi lives on one page" rule and needs an accessible dialog. **To do, Oliver:** set the default amount in his Ko-fi settings to $3. The live page name is `odsens` (display name OddSense) — `.env.example` now seeds that; the webhook's verification token, when S2.1 comes, goes into Vercel's settings, never into chat or the repo.
- 2026-09-18 — **Support fix pass MERGED:** PR #25 squash-merged as `db14c17` on `main`, **no tag** (fix pass); ADR-0042 (D1–D7) flipped to Accepted on the branch (`cc81417`) before the merge; DESIGN.md v1.11; `AmountPicker` is now `KofiCard`. Built and gated in one session (ultracode off; the two-session rule is for slices). Gates on `b13c303`: design-fidelity, frontend, security, backend, supabase, deploy PASS first time; spec-drift ❌ — eight operative doc lines / comments still described the old picker (03 §7 O-8 + O-9, 00 §S1.5b AC1 gloss / Demo / Risks, 00 S1.9.AC8 + moved stubs, O-19 in 02 and 00, the 03 `KofiPanelSlot` source cell, three docblocks) → fixed in `235c158` (docs + three comments only) → spec-drift PASS. Every verdict is a PR comment; the seven final `GATE:` lines are in the PR body. Local chain + CI green (lint, unit 2822, db 1813, build, e2e 116). David saw the preview and said merge. **Production verified** on `https://odsens.com` (built from `db14c17`): `/support` has no picker, one enabled TIP ON KO-FI button, "on Ko-fi ↗" → `https://ko-fi.com/odsens`, nothing from Ko-fi before a click; a browser click-through with ko-fi.com blocked showed the card replaced by one `iframe[title="Ko-fi"]` with focus on "← Back", and Back restoring the card with focus on the button; headers and CSP unchanged; client chunks clean; crons 401; `www` 308. `staging` realigned with `main` (`6056e54`, tree-identical). `/support` first-load JS is 1.2 KB gz smaller than before. **Next: S1.6 — Videos.**
- 2026-09-18 — **From this pass's gates (none blocking; they join the "S1.5b follow-ups" fix pass).** Design questions for DESIGN.md §11.4 (Oliver / David): the "← Back" hover look (built: underline) and that it has no pressed look; the bar's padding (4 / 16px → a 54px bar); the bar's "on Ko-fi ↗" link is underlined at rest, unlike the §5 ghost button; the gold focus ring on the bar's controls overlaps the bar's top line by about 1px; the loading shell pads both slabs 24px where the real "What it pays for" slab is 20px. Accessibility: `KofiPanelSlot`'s `aria-live` wrapper now appears together with its text, which screen readers often do not announce — focus landing on Back inside the region named "Tip on Ko-fi" already gives the context; drop the attribute or keep a persistent live node. CI: `supabase/setup-cli` with `version: latest` hit a GitHub rate limit on `main` (the docs-only #24 run went red in the `db` job's setup step and passed on re-run) — pin the CLI version in `.github/workflows/ci.yml`. Housekeeping: `CLAUDE.md` still calls DESIGN.md "v1.4" and `.claude/skills/design-fidelity/SKILL.md` "v1.3a" (now v1.11) — David's call, since those are instruction files.

## S1.6 build notes (2026-09-18)
- 2026-09-18 — **S1.6 Session A (build pass, branch `feat/S1.6-videos`).** The YouTube channel is on the site: an hourly sync fills a new `videos` table; `/videos` is real (big click-to-load player, "Up next", a "MORE VIDEOS" grid, the Shorts row, an empty state) and replaces the placeholder; Home gains "Latest videos" (the two newest long videos) beside a "Find me" list with the gold tip panel under it; `/admin` gains a videos list where Oliver can hide a video or say "this is / isn't a Short". Nothing from YouTube loads until someone presses play, and then only from `youtube-nocookie.com`. Findings → **ADR-0043** (Proposed, D1–D20), in plain words:
  - **D1 — Oliver's "Short / not a Short" choice has its own column** (`videos.is_short_override`, empty = "let the rule decide"). The site still reads the one `is_short` flag; the sync never touches Oliver's choice, and an "Auto" button hands a video back to the rule (≤ 60 s or `#shorts`).
  - **D2 — the sync collects everything first and writes last.** A live or upcoming stream is not stored until it has ended (the sync never deletes, so a stub would have stayed forever); if any YouTube call fails, nothing is written and the usual "sync failed" notice goes out. Without an API key the sync still adds new videos from the public feed but never overwrites details it already has.
  - **D3 — the public feed is XML**, so the shared HTTP helper gained a text twin (`fetchText`, same timeout / retries / key redaction) and the feed is read with a small hand-written parser — no new package.
  - **D4 — a new small client piece, `VideoStage`, owns "which video is in the big player"** (`/videos?v=<id>`). The page itself stays cached; the newest video is in the HTML; a link with `?v=` switches after the page wakes up, and the text under the player keeps a fixed height so nothing jumps. Without JavaScript the link just reloads the default.
  - **D5 — an "Up next" row is one link.** The little facade in the row is a picture, not a second button (a button inside a link is invalid and fails the accessibility check).
  - **D6 — the shared video type lives in `lib/videos.ts`** (safe for browser code) and the data layer re-exports it.
  - **D7 — play-button sizes follow DESIGN.md:** 88px on the big player, 44px in "Up next", 56px on Shorts **and on grid / Home cards**. The engineering spec had said 88px on cards while citing the DESIGN.md line that says 56 — DESIGN.md wins; it was not edited.
  - **D8 — the test seed has seven videos** (one hidden, one Short, five long) so "Up next" swapping, the grid and Home's "two newest" can be checked without touching data.
  - **D9 — the empty-state test hides every video from `/admin`** (the way Oliver would) instead of wiping the table, because a wipe never refreshes the cached page. The same test proves a moderator cannot hide videos and that hiding works end to end. "No videos" means "none visible".
  - **D10 — the "a failed sync notifies the admins" check for YouTube** is a named test now (T-ACT-74, youtube leg, delivery included).
  - **D11 — the `/admin` videos list:** Hidden and Short switches per row, "Auto" only where Oliver has overridden the rule, "Saved." after every change (the projects pages' toast, now on the dashboard too); moderators see the switches greyed out.
  - **D12 — the `/videos` layout:** title, the line "Mostly me explaining bad decisions.", a "Subscribe on YouTube" button → the big player with its title / views / date / first paragraph of the description / "Watch on YouTube" and four "Up next" rows → "MORE VIDEOS" → Shorts. One cached read feeds both `/videos` and Home.
  - **D13 — no YouTube players inside project descriptions.** The editor's YouTube button keeps inserting a link. (ADR-0039 had left this to S1.6.)
  - **D14 / D15 — wording only:** the security rule's "where are iframes?" check now names the video facade beside Ko-fi; the Home row is described as built (videos | Find me over the tip panel; one column under 900px); `search` struck from the data-model's YouTube row (never used — 100 quota units a call).
  - **Test-coverage floor.** The global floor the test plan asks for "from S1.6" is now enforced (75% lines / 70% branches on the combined run; measured 92.7 / 82.2 on 2026-09-18). No ADR item — the config was brought up to the spec.
  - **Layout shift measured on the production build (2026-09-18; slow network, slow CPU, webfont held back 2 s; 360–430 and 1280 wide).** `/videos` and a direct `/videos?v=` load: 0.006 or less everywhere. Home: 0.00–0.03, except 0.22 at 412 and 430 wide — and that comes from the featured-project hero re-wrapping when the font lands (code from S1.2, untouched here), not from the new videos row, whose rows hold their height in both fonts. The cure is the font-fallback metrics item already queued under "S1.5b follow-ups"; it is not built in S1.6.
  - **Up next on phones (D24).** Picking an Up next video no longer jumps a phone back to the top of the page (it did once the site had refreshed the page); the pick now changes the address bar without asking the server anything.
  - **Build-pass self-review (D21–D23).** A sync that is mid-run when Oliver flips a Short switch no longer overwrites his choice; the Up next duration chip sits flush under the play button instead of on top of it; the "CLICK TO LOAD YOUTUBE" chip shows only on thumbnails wide enough for it on one line (312px), never wrapped over the play button. Deferred design question: should the Up next duration move into the row's text line instead?
  - **D17 — small things about the YouTube connector, written down as built:** if YouTube ever echoes our request back in an error, the API key is blanked out *before* the message is shortened (so not even the start of a key can leak) — this now protects every connector; a feed entry may have no thumbnail and a channel may hide its subscriber count (both are "unknown", never a made-up value); the two helpers the "Seen on" slice will need (read a video id out of a link, ask YouTube for a link's title) were built and tested now, and stay on that later slice's checklist.
  - **D18 — exactly what a sync run writes:** only videos it asked about; a video YouTube does not return (deleted, private, live) is left exactly as it is; a brand-new row never overwrites one that appeared in the meantime; without an API key a known video only gets its title / date refreshed (and its Short flag re-checked against the new title); the cached pages are refreshed only when something actually changed; each run logs how much YouTube quota it used, even when it fails. One known limit: **without an API key the sync cannot tell a live stream from a video**, so it may list a stream; Oliver can hide it. Production runs with a key.
  - **D19 — small `/videos` details:** the line under a video reads "date · views" everywhere; "Watch on YouTube ↗" opens a new tab so a playing video isn't lost; screen readers hear the length with the play button ("Play …, 12 minutes 4 seconds"); the hovered "Up next" row's small text steps up a shade so it stays readable. DESIGN.md's "Grid of older videos below on phone" is read as "the list and the grid stack under the player on a phone" — the MORE VIDEOS grid shows at every width; DESIGN.md was not edited.
  - **D20 — database guard rails on the videos table:** a YouTube id must be 11 valid characters, counts and durations can't be negative.
  - **D11 / D15, as built:** the `/admin` list shows a HIDDEN / LIVE pill per video (no "short" pill — the Short switch says ON / OFF), "Saved." appears only when the save really worked, and the dashboard's sync table now has a YouTube row with its own "Sync now". On Home the "@OdSens on YouTube ↗" link opens a new tab; before the first project sync (the bare Home) the videos row is not shown at all.
- 2026-09-18 — **Notes for whoever reads the preview:** the staging database is not seeded and may have no YouTube key, so the preview's `/videos` shows the empty state until a sync runs; the authorised cron 200 cannot be checked from a local `.env` (same limit as S1.5b). Fixtures under `tests/fixtures/youtube/` are hand-made in YouTube's shape — never recorded with a real key. In a no-key run no video has a duration, so none is classed as a Short until a keyed run fills them in.
- 2026-09-18 — **Deferred design questions (Oliver / David — none blocks S1.6):**
  - **Shorts tile size while playing.** A Short plays inside its 104px-wide tile, as specified; YouTube's player is cramped under 200px. Letting the tile grow when it plays would be a new DESIGN.md rule — not built.
  - **Home prototype extras not built:** the project counts beside "Find me" ("18 projects" — placeholder numbers in the mock; Home reads no such data), 32px platform marks (the component allows 24 / 26 — 26 used), whether the "@OdSens on YouTube →" head link opens a new tab (built: yes, with "↗"), and the row's bottom rule directly above the footer's top line (left off to avoid a doubled 4px line). No phone mock exists for this row — the 390 layout is derived from DESIGN.md §6 breakpoints (one column: videos, Find me, tip panel).
  - **Home phone gutter.** The new Latest videos row uses DESIGN.md's 24px phone gutter; the older hero and Featured sections use the phone mock's 20px, so below 600px the new row sits 4px further in. Pick one for the whole page in a design pass.
  - **"views · date" or "date · views".** The `/videos` mock draws views first; the build uses "date · views" everywhere so a video with no view count yet starts the same way (D19). Say if the mock's order is wanted.
  - **The "CLICK TO LOAD YOUTUBE" chip on narrow cards** (about 200–300px wide — tablet grids, Home between 900 and 1150px) wraps to two lines and can touch the play block. A fix for this PR's verify pass, not a design decision — listed so nobody re-reports it.
  - **YouTube players inside Markdown** — reopened only if Oliver asks (D13).
- 2026-09-19 — **S1.6 MERGED:** PR #27 squash-merged as `12c46ed` on `main`, tag `v0.7`; ADR-0043 (D1–D25) flipped to Accepted on the branch (`70fd5ea`) before the merge. Gates: round 1 on `0dddd85` — frontend / security / backend / supabase / deploy PASS; spec-drift ❌ (a Short's gold duration chip sits top-left, as the pass-3 Shorts artboard draws it, while 03 §2.6 said bottom-right and nothing recorded it — docs only); design-fidelity ❌ ("Watch on YouTube ↗" had no gold pressed state, and it and the Home "@OdSens on YouTube ↗" link hovered with a thin underline instead of DESIGN.md §5's 2px). Fix round `e587f8a` (two CSS rules + ADR-0043 D25 + the 03 row; merged into `staging` as `5d13e56`, merge commit, no force) → both PASS. All verdict tables are pasted in the PR comments and the final `GATE:` lines in the PR body; CI green on the merge head. Production checked after the deploy: `/`, `/videos`, `/projects`, `/support` and `/admin` 200 (`/admin` shows the admins-only gate to a signed-out visitor), `/api/cron/sync-youtube` 401 without the secret, `frame-src` still `youtube-nocookie` + Ko-fi only. `/videos` and the Home column show the empty state until the first hourly sync (`27 * * * *`) or a "Sync now" from `/admin`. The signed-in `/admin` videos list was not checked from here (no admin session) — it is covered by T-E2E-47 in CI.
- 2026-09-19 — **S1.6 follow-ups (from the Session B gates; none blocking, none built — a fix pass of their own beside the "S1.5c follow-ups" and "S1.5b follow-ups").**
  - **Voice control on the play button (frontend A4 — the one with a user cost).** The button's spoken name is "Play <title>, <length>", but the words a visitor sees on it are "CLICK TO LOAD YOUTUBE", so someone using voice control who says those words gets no match (WCAG 2.5.3). Cheapest fix: put the visible words in the name. Needs a 03 §2.6 edit + ADR.
  - **A shared `/videos?v=…` link shows the newest video for about a second before the chosen one** (frontend A2) — no layout jump, and it follows from ADR-0043 D4 (the cached page cannot read `?v=`). A small inline script or a per-video static page would remove it; either needs an ADR. The same load also fetches the newest video's thumbnail at high priority and throws it away (A3).
  - **Sync alerts don't say why YouTube refused** (backend A1): a quota error is stored as "… → 403" only; adding a short, already-redacted excerpt of YouTube's reason would make the email something Oliver can act on. Related, accepted as designed: a run with a key still fails when only the RSS feed is down (A2); a `full` walk with several slow retries could in theory pass the 300 s limit — safe, nothing is half-written (A3); a row whose Short switch changed mid-run counts as "updated" and refreshes the page once for nothing (A4); `listVisibleVideos` has no paging, so 1,000 videos is the ceiling — the channel has about 21 (A5).
  - **Hardening, optional:** a database CHECK that `thumbnail_url` starts with `https://i.ytimg.com/` (security A2 / supabase A2 — today only the sync enforces it, and only admins can write); nothing in the database keeps `is_short` in step with `is_short_override` if an admin writes the table directly (supabase A3 — ADR-0043 D1 rejected a trigger on purpose); a `sandbox` attribute on the YouTube frame (security A1 — fragile with YouTube, test in e2e first); the test-only `YOUTUBE_API_BASE` / `YOUTUBE_RSS_BASE` overrides are honoured in production too, so those names must never be set in Vercel — a question for S1.10 (security A3).
  - **Small code / doc tidies:** `ShortsRow` does not need `'use client'` (frontend A1 — 03 lists it as a client file, so changing it needs a 03 edit + ADR); the "No preload link" comment in `VideoFacade.tsx` is wrong — React does emit one for the big thumbnail (A3); RSS-only rows store a 480px thumbnail that the big player upscales until a keyed run replaces it (A3); docblocks in `lib/jobs/syncYoutube.ts` and `lib/actions/videos.ts` still cite the old 04 §3.3 step numbers (spec-drift A3); the 03 `VideoFacade` Tokens cell omits `--slab-sunk`, `--line-soft`, `--chalk` (A6); a refused or failed `/admin` video switch is not announced — no error surface on `Toggle` (frontend A5, recorded in ADR-0043 D11).
  - **For DESIGN.md's next revision (Oliver / David):** say "top-left on Shorts" in §11.1 / §11.5 so nobody has to find ADR-0043 D25; §5 words the ghost-link hover as "a 2px underline via inset shadow" while these links and the Ko-fi card use a 2px text underline — same look, say either is fine; the Ko-fi "← Back" control still hovers with a thin underline; the Up next length chip covers part of the play block's shadow — moving the length into the row's text line would restore it (already in "S1.6 build notes"); `.claude/skills/design-fidelity/SKILL.md` still cites DESIGN.md v1.3a / v1.4 — the file is v1.11.
  - **Not yet measured on real data:** Lighthouse LCP on `/videos` with real thumbnails (the seed's thumbnails 404 locally and staging had no videos) — re-run once production has synced (frontend A6). The signed-in cron 200 was not exercised on the preview (deploy A1 — it would have written to staging).
  - **Gate process (owner `test-engineer`):** the `db` test project resets the one local database on every run unless `SKIP_DB_RESET=1`, so gate agents run in parallel trampled each other this round (failed builds, fixture rows baked into a page — none a code fault). Put `SKIP_DB_RESET=1` in every gate brief except `supabase-reviewer`'s, or lock the reset. Also `VERCEL_AUTOMATION_BYPASS_SECRET` is not in the local `.env`; `vercel curl` works without it (deploy A2).
- 2026-09-19 — **Wrong YouTube channel id (found at the S1.6 production check; David confirmed @OdSens is the channel).** The channel id in `docs/spec.md` §2, `.env.example` and Vercel (`UCo3X_c7MqfC_ub-sMJZmmOA`) was never Oliver's — the @OdSens handle resolves to `UCR7s27wznG-Utxm1OLSvqow`. Timeline (UTC): 06:27 the first production sync failed (YouTube answered the feed with a 404 that once); 07:27 the second succeeded and inserted 53 of the other channel's videos, which showed on `/videos` and Home; ~07:30 `YOUTUBE_CHANNEL_ID` corrected in Vercel (Production / Preview / Development) and production redeployed; the wrong rows are removed by migration `20260919073500_videos_wrong_channel_cleanup.sql` (rows created before 07:35Z — the sync never deletes and the table has no channel column). No gate caught it because every test uses a seed channel id and staging never ran a sync. **Follow-ups (join "S1.6 follow-ups"):** (a) `syncYoutube` could refuse to write when the feed's channel handle / title is not the expected one, or the `/admin` sync row could show the channel's name so a wrong id is visible at a glance; (b) a local `.env` made from the old `.env.example` still carries the old id — replace it by hand.
- 2026-09-19 — **DECIDED (David): build order — Seen on before Skins + Art (ADR-0044).** After the S1.6 merge David flipped the next two slices: S1.8 — Seen on is built next (tag `v0.8`), S1.7 — Skins + Art after it (tag `v0.9`). Slice numbers, AC ids and test ids stay as they are; nothing in either slice's scope changed; Seen on needs nothing from Skins + Art (it depends on S1.2 + S1.6 only). The S1.7 Session-A prompt written at the S1.6 close-out is void.

## S1.8 build notes (2026-09-25)
- 2026-09-25 — **S1.8 Session A (build pass, branch `feat/S1.8-seen-on`; begun 2026-09-19, cut off by the weekly limit, finished 2026-09-25).** Other people's videos and posts about the mods are on the site: Oliver pastes a link on Admin → Mentions, the server reads the page's title, creator, views and date, he assigns it to a project (or to "About OddSense generally") and publishes. A published mention shows on the project's page (a SEEN ON row), on Home (an IN THE WILD strip with the reach line "1.2M VIEWS · 2 VIDEOS · 2 CREATORS") and on `/seen-on` (three totals, a filter by platform and by project, the grid). YouTube mentions play in place; every other platform links out with a worded chip and never loads a picture from that site. YouTube view counts refresh every hour. The footer carries its second dry line everywhere. Everything is written down in ADR-0045 (Proposed, D1–D30); the code's own notes were reconciled into that numbering when the session resumed.
  - **D1–D3 — the mentions table.** A mention is never deleted with its project (folding two listings into one carries the mentions across); reordering the Home strip is one database call that either applies fully or not at all.
  - **D5–D6 — reading a page somebody else chose is the one risky request in the app, so it is fenced in:** only https, no odd ports, no private or local addresses (every address a name resolves to is checked, and every redirect is checked again), one 10-second budget, at most 1 MB of HTML, and only the parsed title / picture / site name come back — never the page. There is deliberately no test switch that could point this read at a private machine (the test switch `OEMBED_BASE` only moves YouTube's oEmbed endpoint). One known limit: a page could in theory answer the check with one address and the request with another (DNS rebinding); with https-only that connection cannot complete, so it is accepted and written down.
  - **D7–D8 — the preview.** YouTube links are read through oEmbed (title, creator) and the Data API (views, date — one video, matched by id, never "the first result"); anything else through the page's Open Graph tags. Whatever goes wrong, Oliver sees the same line — "Couldn't read that page. You can fill the fields by hand." — so the guard never tells an attacker why.
  - **D9–D10 — saving.** The stored link is the clean form (tracking bits removed, any YouTube link → the plain watch link), so the same page cannot be added twice; a YouTube mention keeps its video id; a bad reorder changes nothing.
  - **D11–D13 — the hourly refresh** asks YouTube for view counts 50 videos at a time (one quota unit each), writes only when a number really changed, never blanks a number when a creator hides their count, and gets a "Sync now" button on Admin → Mentions. The "sync stale" alarm for mentions only rings when there is something to refresh.
  - **D14–D15 — one read feeds the three public pages**, and a mention attached to a hidden or draft project shows nowhere (it is not "about OddSense generally" either). `/seen-on`'s filters are in the address bar (a filtered view can be shared), handled by a small client piece so the page itself stays cached.
  - **D16–D17 — the card.** A YouTube mention saved without a video id links out ("WATCH ON YOUTUBE") instead of pretending to play; the card is not one big link (the creator's name and the project name are their own links); every card shows its title under the creator line, as the design pass-3 artboard draws it; the play button on a mention card is the 56px grid-card size, as DESIGN.md §12.7 says (the engineering note that said 88px was written before the card existed).
  - **D18–D22 — small things on the public pages:** the reach line is built from separate Silkscreen labels (the label component refuses more than five words); "1 MENTION" reads singular; the Home strip is four columns with four cards, three otherwise; `/seen-on`'s three tiles show at every width, without the artboard's sublines (02 §2.6 is the binding copy), and the page has no separate reach line — the tiles are the totals; the ODSENS chip is the wordmark in Bungee on the mark's slab; the only way to reach "NOTHING HERE" is a platform × project combination (the project list never offers a project without a mention).
  - **D23–D26 — Admin → Mentions:** Mentions and Suggested views (the Suggested one is the empty v1.5 stub: "NOTHING SUGGESTED / Auto-found mentions will wait here for a yes or no. Not yet."); ADD A MENTION → FEATURED ORDER (drag, arrow keys, or the new Move up / Move down buttons) → ALL MENTIONS (newest added first, so a reorder never shuffles the table; Feature / Unfeature / Hide / Show / Publish per row; "Feature" joins the end of the Home strip) → SYNC; "Saved." after every successful action. A moderator sees the published rows only, read-only, every control greyed out under "Admin only".
  - **D27–D30 — tests:** the admin page is checked at phone width too; the component gallery gained the Seen on pieces; one unit test pins the add-form's markup without a browser (a reading of the "no render tests" rule, not a new test harness); the fixture server can answer a single-video request with its own file; the adapter coverage floor the test plan asked for from S1.2 is now actually enforced.
  - **Test coverage, measured 2026-09-25 (combined unit + db run from a fresh reset):** 137 files / 6125 tests; 93.7 % lines / 84.5 % branches overall; the adapters (the new SSRF guard included) 99.7 % / 95 %. Local chain: lint green (323 test ids, 49 client islands, 120 css files tokens-only).
- 2026-09-25 — **Notes for whoever reads the preview:** the staging database has no mentions, so the preview's `/seen-on` shows the title only, Home shows no IN THE WILD strip and project pages show no SEEN ON row until Oliver adds one; pasting a link on the preview reads the real page (there is no fixture on staging) — a YouTube link needs `YOUTUBE_API_KEY` in the environment for views + date, otherwise title + creator only; the hourly refresh needs the same key.
- 2026-09-25 — **Deferred questions (Oliver / David — none blocks S1.8):**
  - **Official platform marks (Q44)** — still neutral placeholders; the TikTok / Twitch / Reddit marks now appear on cards, so the asset PR matters more.
  - **TikTok / Twitch / Reddit oEmbed** — v1 reads their Open Graph tags (04 §5.4 "not used in v1"); TikTok and Reddit may block the read, in which case Oliver fills the fields by hand (the built path). Adding an endpoint later is an adapter change plus a fixture.
  - **A page in a legacy character set** yields a garbled title in the preview (the read is UTF-8 only); Oliver retypes it.
  - **Reach line on `/seen-on`** — the design lists a reach line under §12.1 and the page shows the totals as tiles instead (02 §2.6). Say if the line is wanted on the page as well.
- 2026-09-25 — **S1.8 MERGED:** PR #31 squash-merged as `82cfe47` on `main`, tag `v0.8`; ADR-0045 (D1–D30) flipped to Accepted on the branch (`e71cd60`, which also added `reorder_mentions` to the data-model's service-role-only list as the schema gate asked) before the merge. Gates: round 1 on `1f1360c` — **all seven PASS first-pass, no fix round** (spec-drift 13/13 · design-fidelity 28/28, DESIGN.md unchanged confirmed · frontend 23 checks, axe 0/0 at 1280 + 390 on all four surfaces, largest first-load delta +7.8 KB gz, the YouTube facade loads nothing before a click · security 23/23 on the link reader's guard and the three admin actions · backend 34 checks on batching, quota and the gather-then-write refresh · supabase 18 with two warnings · deploy on the preview). Every verdict table is in the PR comments and the seven `GATE:` lines in the PR body. CI before the merge needed the e2e job re-run twice on the docs-only flip commit — first a GitHub rate limit while downloading the Supabase CLI (before any test), then the component-gallery screenshot test at phone width over its 30 s default (it had sat at 24–26 s on the last three `main` runs) — fixed with an explicit 90 s budget (`bd92fff`); nothing under test changed. Production checked after the deploy: `/`, `/seen-on` (title only — no tiles, no grid, no empty box), `/projects` and `/projects/phobias` 200, with no SEEN ON row and no IN THE WILD strip yet (production has no mentions until Oliver adds one) and the footer's second line on every public page; `/admin/mentions` shows the ADMINS ONLY gate to a signed-out visitor (200, in place); `/api/cron/refresh-mentions` 401 without or with a wrong secret, 405 on POST; CSP `frame-src` still `youtube-nocookie` + Ko-fi only, HSTS + `X-Frame-Options: DENY`; `vercel crons ls` lists `refresh-mentions` at `37 * * * *` beside the four existing crons. The merge build waited about a minute for the three migrations (`wait-for-schema: ready (37 migrations, 23 relations)`), no manual redeploy. The authorized cron ping was not exercised from here (the secret is not on David's Mac); the first hourly run at :37 writes its own `sync_runs` row, visible on Admin → Mentions → SYNC.
- 2026-09-25 — **S1.8 follow-ups fix pass BUILT** on `fix/S1.8-followups` (PR #33, no tag; ADR-0046 Accepted on the branch before the merge, D1–D6; built and gated in one session — the two-session rule is for slices). What changed, in plain words: on Admin → Mentions the fetched card and the by-hand fields now show the exact web address that will be saved when Oliver presses PUBLISH (a page can claim a different "canonical" address than the one he pasted — now he sees it first; it is plain text, not a link, so nothing is clickable before it is approved); a page that answers the link reader with an error is no longer downloaded in full before the site gives up on it; the filter buttons on Seen on and Projects show the same gold keyboard focus ring as everything else; and four edge cases of the hourly view-count refresh are pinned by tests (exactly 50 / 51 videos per request, a count that is not a number, a "slow down" answer from YouTube mid-run). The local chain was green (lint · coverage 137 files / 6137 tests from a fresh reset, thresholds held · build + bundle check · e2e 134); `staging` merged with a merge commit (`48e3ec5`, tree-identical to the branch); preview `/seen-on` 200, `/admin/mentions` 200 (ADMINS ONLY gate), `/projects` 200, `/api/cron/refresh-mentions` 401. Gates on `d241de4`: spec-drift, security, backend, deploy PASS first-pass; frontend (2 ❌) and design-fidelity (1 ❌) both on the one defect — the phone-width type row on `/projects` (five links overflow) rested 5px left with the first link's focus ring clipped, because the ring's padding on the scroll box moved the snap point → fixed in `07cfbe6` (`max-width: calc(100% + 10px)` + `scroll-padding-inline: 5px`, a `/projects` phone leg on T-E2E-2), both gates PASS on round 2. Advisories recorded for DESIGN.md's next revision (below): the line's 12px vs the card's 13px meta, the word ("Link" — "Saves as" would read truer); taken: the word in sentence case like the field label. The "Someday, not this pass" bullet below is untouched.
- 2026-09-25 — **S1.8 follow-ups (from the Session B gates; none blocking, none built). David decided (2026-09-25) to clear these in a fix pass BEFORE S1.7 — Skins + Art starts, rather than queueing them beside the S1.5c / S1.5b / S1.6 lists.**
  - [x] *Cleared in PR #33 (ADR-0046 D1 / D2).* **Show the link that will be stored (security, the one with a visitor cost).** For a non-YouTube page the preview keeps the page's own canonical address (`og:url`) and PUBLISH stores that, but the preview card shows title, creator and date and not the link — a page could name a different address than the one Oliver pasted and he would never see it (`components/seen-on/MentionPreview.tsx:154-198`, `MentionPreview.draft.ts:195`). Fix: show the stored link on the card (and in the manual fields) so he approves what visitors will click. Needs a 03 §2.8 `MentionPreview` note + ADR (`web-quality` / `design-fidelity`).
  - [x] *Cleared in PR #33 (ADR-0046 D3).* **Error pages are read in full (backend #29).** `fetchPage` caps a successful page at 1 MB but a 4xx / 5xx body is buffered whole before the 300-character excerpt is cut (`lib/adapters/http.ts:342`) — bounded only by the 10 s budget. Fix: read the error body through the same capped reader when `maxBytes` is set, or discard it. `backend-robustness`, no schema, ADR note on 04 SC-09.
  - [x] *Cleared in PR #33 (ADR-0046 D4 — CSS only).* **Filter buttons lose the gold focus ring (frontend #13 — pre-existing since S1.2, visible on `/seen-on` and `/projects`).** `components/projects/FilterBar.module.css:34` draws its own 2px `--line-soft` outline, which beats the global 3px gold ring. Fix: a `:focus-visible` rule on `.filter-bar-type` (DESIGN.md §5). `design-fidelity`.
  - [x] *Cleared in PR #33 (ADR-0046 D5 — test legs on T-ADP-13 / T-ACT-54; no code change was needed).* **Three boundary tests (backend #34, `test-engineer`):** exactly 50 ids → one request and 51 → two (only 2 / 120 / 999 are pinned today); a non-numeric `viewCount` reaching the job end-to-end (today adapter-only); a 429 + `Retry-After` on one `videos.list` batch inside the job, counted as one unit.
  - [x] *Checked 2026-09-25 (PR #33, read-only).* **After the production deploy (`vercel-ops`):** confirm `vercel crons ls --scope studiobing` lists `/api/cron/refresh-mentions` at `37 * * * *`, and exercise the authorized cron ping once with the production secret (never checked from David's Mac — the secret is not on disk). → `vercel crons ls` lists it at `37 * * * *` beside the four others; the production runtime log (`vercel logs <deployment> --since 3h`) shows Vercel's own scheduled call at 15:37:19Z on the current deployment answered 200 with `refreshMentions` `done` (`trigger: cron`, `mentions: 0`, `units: 0` — nothing to refresh until Oliver adds a mention), so the secret-bearing ping works without anyone pulling the secret; the earlier 401 / 405 lines in the same log are the S1.8 close-out's own checks.
  - **Someday, not this pass (join the consolidated pass after S1.7):** `refreshMentions` has no wall-clock budget — more than ~300 YouTube mentions all retrying could pass the 300 s limit and leave the run marked as crashed until the 15-minute lock expires (backend #31); the public mentions read has no page size, so PostgREST's 1,000-row ceiling would silently drop the oldest mentions from `/seen-on` and the totals (backend #32); the preview's two YouTube calls keep the shared loop's three retries, so a dead YouTube could hold the Fetch button ~94 s before the Open Graph step (backend #30 — per spec, an ADR if the form ever times out); `reorder_mentions` has no 200-item cap in the database (supabase #11 — schema-only, recorded in ADR-0045 D2); the admin table's title link is 25 px tall at 1280 (frontend #15 — the row is 44); mobile-simulated LCP of 4.6–4.9 s on every page is the site's render-blocking stylesheets, not S1.8 (frontend #19 — `web-quality`, site-wide); the ODSENS wordmark chip reuses `--text-body-phone` for its 16px — a `--text-mark-word` token would read truer (design #10).
  - **For DESIGN.md's next revision (Oliver / David):** the admin preview card (and the by-hand fields) now carry a link line — the word LINK then the address to be stored, `--mute-dim` 12px, clipped to one line — which the pass-3 artboard does not draw (ADR-0046 D2; built so Oliver approves the exact address visitors will click); §12.1 has no hover rule for a link-out mention card (built: the arrow chip turns indigo, the name and project links underline, no card lift); the admin Suggested tab shows a COMING LATER label and no count (the artboard draws a count that AC10 forbids); a card's date hides while a count is shown so the row keeps 44 px (ADR-0045 D16 records why).
  - **Standing rule (security):** DNS rebinding is accepted unpinned for the mention preview because it is https-only and admin-only (ADR-0045 D6). Any future page read that is not https-only or not admin-only must pin the resolved address before connecting.
  - **Gate process note:** the shared local database still trips parallel gates even with `SKIP_DB_RESET=1` — this round `supabase-reviewer`'s fold-test lane and `backend-reviewer`'s curseforge stale leg failed only from rows another gate left behind; both were green in CI from a fresh reset. Give gates disjoint file lists, or run `supabase-reviewer` first and the others after its reset.

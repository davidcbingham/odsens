# Open Questions (running list)

Answered items get folded into `spec.md` and struck here.

## Content & ownership
1. ~~**Source of truth for projects**~~ — **Answered:** Everything on Modrinth appears on the site, **plus exclusive on-site projects** (Oliver authors name/description/gallery and uploads the file). Modrinth parameters = the schema. Four categories: mod, datapack, resource pack, plugin.
2. ~~**CurseForge overlap**~~ — **Answered:** Little content on CF, all also on Modrinth. **Get an API key**; sum download counts across both. Placeholder in `.env`.
3. ~~**Scratch games and YouTube**~~ — **Answered:** **No Scratch** on the site. YouTube: yes (embed).
4. ~~**Skins**~~ — **Answered:** He doesn't use any skin site. **Native Skins section** — design deferred to a dedicated discussion.
5. ~~**Non-Minecraft work**~~ — **Answered:** See #1.
6. ~~**Blog / posts / devlogs**~~ — **Answered:** Deferred, maybe never.

## Editing workflow
7. ~~**How does Oliver edit content?** Options: (a) a small admin UI in the site (login → forms) — most friendly; (b) markdown/JSON files in the repo edited via GitHub — teaches git, no UI to build; (c) Supabase dashboard directly — zero build, but clunky. Preference?
8. ~~**Will Oliver code?** Is part of the goal for him to learn web dev on this project (affects framework choice and how much we document), or should it be a finished tool he uses?

## Comments & moderation
9. ~~**Moderation model**~~ — **Answered:** See earlier batch — admin setting; multi-mod; notifications toggle.
10. ~~**Replies / threads / reactions**~~ — **Answered:** **Threaded comments + likes.** Threads let Oliver reply to people; likes surface feedback on what resonates. (Updated 2026-08-16.)
11. ~~**Notifications**~~ — **Answered:** Yes, but **channels + infrastructure need a dedicated design session** (email / Discord / in-app / push; per-event types). Added to Future Design Sessions.

## Donations
12. ~~**Platform**~~ — **Answered:** **Ko-fi**, with **maximum usability, minimum clicks** — embedded panel, no redirect where avoidable. Also: 12b **Custom Orders / commissions** (leverage Ko-fi Commissions), 12c **donor leaderboard** (idea queue).

## Identity & aesthetic
13. ~~**Name presentation**~~ — **Answered:** **OddSense** = Oliver (his username on the site) and his Minecraft character. **odsens** = the website/brand — distinct from the person and character.
14. ~~**Existing art assets**~~ — **Answered:** Yes. Oliver will work with Claude in a separate **Claude Design** session to produce a `DESIGN.md`. Process + advantages documented in `docs/design-process.md`.
15. ~~**Tone**~~ — **Answered:** **Playful and cartoony. Fun, relaxed, inviting.**
16. ~~**Reference sites**~~ — **Answered:** No named references. After the functional spec is complete: abstract it into site/functionality types → Claude searches for examples → Oliver critiques good/bad. Process in `docs/design-process.md`.

## Technical / accounts
17. ~~**Framework**~~ — **Answered 2026-08-17:** **Next.js (App Router, TS) on Vercel + Supabase**, plain CSS tokens (no Tailwind), Resend for email. Full reasoning in `docs/framework-decision.md`.
18. ~~**Google OAuth**~~ — **Answered:** Google Cloud project under **david@studiobing.com**; hosts both the OAuth client and the YouTube API key. Step-by-step in `docs/setup-google-cloud.md`.
19. ~~**Vercel/Supabase org access**~~ — **Answered:** Discussed 2026-08-16 — see tradeoffs below. **Recommendation:** start with StudioBing logins only; Oliver operates through GitHub + the admin UI (Vercel auto-deploys from GitHub). Add him to Vercel/Supabase later only if needed.
20. ~~**Analytics**~~ — **Answered:** Options + effort table in `docs/analytics-options.md`. Recommend Vercel Web Analytics + Speed Insights + custom events + own Supabase counters at launch; daily external-stats snapshots as first enhancement.
21. ~~**Squarespace DNS**~~ — **Answered:** Confirmed: registration only. Clean sheet; old Cloudflare project is deprecated.

## Privacy
22. ~~**What personal info is OK to publish?** First name? Age? Nothing beyond the handle? (Scratch bio currently says "I'm 15 and in 9th grade.")

## New (from 2026-08-16 answers)
23. ~~**Content without a native home**~~ — **Answered:** **No PII on the site.** Applies to visitors too: on first sign-in every user must **choose a handle** (+ optional profile image); Google name/email are **never** displayed or used as a display name. Oliver's own presence = handle only.
24. ~~**CurseForge API key**~~ — **Answered:** Yes — key goes in `.env` as `CURSEFORGE_API_KEY`.
25. ~~**YouTube Data API**~~ — **Answered:** Yes — same Google Cloud project as OAuth. See `docs/setup-google-cloud.md`.
26. ~~**Roblox**~~ — **Answered:** No Roblox.
27. ~~**Oliver's setup**~~ — **Answered:** VS Code + Claude Code working on his laptop; has a GitHub account (unused). David will set up the clone and give **full repo access** to both. David front-loads effort, then transitions to Oliver with minimal maintenance pushes. Skills may expand to cover major updates, design decisions, dev work.

## Future design-detail sessions (agreed, not yet scheduled)
- **Skins section** — how skins are shown, viewer, download, metadata (from Q4)
- ~~**Notifications**~~ — **designed 2026-08-17** → `docs/notifications.md`
- **Comments** — threaded + likes UX, notification hooks, moderation UX (from Q10)
- **Custom Orders + Workrooms** — intake (designed), workroom flows, safety rules, retention, client email opt-in (Phase 2)
- **Asset IA** — categories, file types, pixel dimensions for `assets/brand/` (from Q14)

## Idea queue (not committed)
- **Donor leaderboard** — ranks supporters by total tipped; **ties to site account names** (Google-authenticated users). Needs a way to link a Ko-fi payment to a site account (e.g. Ko-fi webhook `email`/`from_name` match, or a "link my Ko-fi" step / code in the tip message). Privacy: opt-in display name.
- **Custom Orders** — see design session above.

## Post-spec workstreams (agreed 2026-08-16)
- ~~**DESIGN.md** via Claude Design session with Oliver~~ — **done** (`/DESIGN.md` v1.1, Crate Poster). ~~Review pass~~ **done** → `docs/design-review.md`; gaps logged as Q28–32.
- **Inspiration hunt** from abstracted functionality → `docs/design-process.md`
- ~~**Site management skills**~~ — **spec'd 2026-08-17** (12 skills mapped to Oliver's moments) → `docs/site-management-skills.md`; written for real once the first build exists
- ~~**Framework choice**~~ — **done** → `docs/framework-decision.md`

## From the DESIGN.md review pass (2026-08-16) — see `docs/design-review.md`
28. ~~**Card border contrast**~~ — **Answered (pass 2):** Card/panel outlines lifted one step to `--line-soft #2C3A4B`; `--line` kept for internal dividers.
29. ~~**User notifications**~~ — **Answered (pass 2):** **Cut from v1** — no user inbox; admin gets email. Bell spec retained for later.
30. ~~**YouTube Shorts**~~ — **Answered (pass 2):** **Shorts row added** under the long-form grid (9:16 facades).
31. ~~**Hero**~~ — **Answered (pass 2):** Unchanged in pass 2 (featured-project takeover hero) — treated as confirmed.
32. ~~**Second Claude Design pass**~~ — **Answered (pass 2):** **Done 2026-08-17** — `design/claude-design-export/pass-2/`, `DESIGN.md` v1.2 §11.
## From pass 2 (Claude Design's open items, 2026-08-17)
33. ~~**Ko-fi tip event**~~ — **Answered 2026-08-17:** v1 supporters wall = **leaderboard: handle + amount**. Linking a tip to a site account: server-side **hashed-email match** (never displayed/stored raw) → else handle typed in the Ko-fi message → else "Anonymous · $X". Amount shown only when linked or Ko-fi `is_public`. DESIGN.md note updated (was "no amounts").
34. ~~**Handle validation heuristic**~~ — **Answered 2026-08-17:** **No name detection.** Structural validation only (3–20, `[A-Za-z0-9_]`, unique, reserved words, no `@`); heavy guidance copy at handle creation + in the privacy/comments pages. Design's "looks like a real name" state dropped.
35. ~~**Comment limits**~~ — **Answered 2026-08-17:** Yes — 1000 chars, one link per comment.
36. ~~**Under-13 line** on the Privacy page ("don't sign in") — needs David's call before ship.
37. ~~**Still-missing art**: project icons, in-game screenshots, rendered 3D skin busts (build-time via skinview3d).

## From data-model design (2026-08-17) — see `docs/data-model.md`
38. ~~**Auto-hold on reports**~~ — **Answered 2026-08-17:** Yes — auto-hold at ≥3 reports.
39. ~~**CurseForge id mapping**~~ — **Answered 2026-08-17:** Yes — manual CurseForge id entry in admin.
40. ~~**Comment edit window**~~ — **Answered 2026-08-17:** Yes — 15-minute **edit** window after posting; then delete only.

## Setup to-dos before build (David)
- [x] Google Cloud project + OAuth client + YouTube key — done, Supabase Google provider configured via `supabase config push` (`docs/setup-google-cloud.md`)
- [ ] CurseForge for Studios API key → `.env`
- [x] Supabase project **`odsens`** created (ref `dllbekulbimblrsrxuyv`, us-east-2) and linked from the repo (`supabase/config.toml`) — treated as **production**. Staging = **Supabase Branching** (decided; set up at first preview deploy) — **2026-08-20:** a persistent `staging` branch, not a branch per PR (ADR-0010)
- [x] Vercel project `odsens` linked (id `prj_fTdiX6oYxyQ8CnAmzSzKnCb74MkU`), Standard protection on, env vars seeded, `CRON_SECRET` generated
- [ ] Ko-fi account (18+, StudioBing) with page `oddsense` if available (phase 2)
- [x] Resend: Vercel integration installed (`RESEND_API_KEY` in all envs + local `.env`), domain verified (DKIM/SPF/MX), sender `allay@odsens.com`, test send OK
- [ ] **DMARC record** (Squarespace → Domains → odsens.com → DNS settings → Add record):
  - Type: `TXT` · Host/Name: `_dmarc` · Data/Value: `v=DMARC1; p=none; rua=mailto:david@studiobing.com; fo=1` · TTL: default
  - (`p=none` = monitor only; reports go to your inbox weekly. Tighten to `p=quarantine` later if desired.)
- [ ] **Inbound for allay@odsens.com** so replies to notifications don't bounce — pick one:
  - (a) *Squarespace email forwarding* (Domains → odsens.com → Email → Email forwarding): forward `allay@odsens.com` → `david@studiobing.com`. Squarespace will add its own MX records for `odsens.com` automatically (root MX only; the `send.odsens.com` MX for Resend is unaffected).
  - (b) *Resend Receiving*: Resend dashboard → Domains → odsens.com → enable receiving; add the MX record it shows (Type `MX`, Host `@`, Priority `10`, Value `inbound-smtp.<region>.amazonaws.com`) — then inbound mail arrives via Resend webhooks/dashboard.
  - Recommendation: **(a)** — simplest; you read replies in Gmail. Tell me when done and I'll set `Reply-To: allay@odsens.com` in templates.
- [ ] **Discord**: confirm Oliver has his own server; create a private `#odsens-admin` channel + webhook URL → `.env` `DISCORD_WEBHOOK_URL` (also settable in admin Settings)
- [x] Docker runtime on David's Mac — OrbStack installed, local Supabase verified (`docs/dev-tooling.md`)
- [ ] Decisions: Q33–40
- [x] Enable **Supabase Branching** on `odsens` + install the Supabase **GitHub integration** (`davidcbingham/odsens`) + the Supabase **Vercel integration** — REQUIRED before S1.1 (ADR-0006; at S0 the preview runs on placeholder anon/service-role values) — **done 2026-08-20:** Branching on, both integrations installed; previews run on the persistent `staging` branch (ADR-0010 supersedes ADR-0006; automatic per-PR branching is turned off)
- [ ] **Persistent `staging` branch — three dashboard steps (ADR-0010):** Supabase → GitHub integration → turn **off** automatic per-PR branching · Vercel → Preview environment → paste staging's `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY` (values never via chat) · Google Cloud console → OAuth client → add `https://oihrxwqarwllvsyllczo.supabase.co/auth/v1/callback` as an authorized redirect URI. Until all three are done a PR preview may still run on an ephemeral branch (`deploy-checker` reports which ref it found).
- [x] Vercel: enable **Protection Bypass for Automation** (Project → Deployment Protection) so `deploy-checker` can fetch protected previews — **done 2026-08-20:** enabled; `deploy-checker` sends the secret as the `x-vercel-protection-bypass` header (`.env.example` commented tooling line `VERCEL_AUTOMATION_BYPASS_SECRET` — documentation only, not a `lib/env.ts` / T-UNIT-35 name)
- [x] Vercel: remove `CURSEFORGE_MEMBER` from all environments (dropped from `.env.example` at S0, 04 SC-16) — **done 2026-08-20:** removed from every Vercel environment

## From "Seen on" + Workrooms (2026-08-17)
41. ~~**Mention click behaviour**~~ — **Answered (pass 3):** YouTube mentions embed inline (facade → player, indigo-lift outline, "on YouTube ↗" ghost link); other platforms link out.
42. ~~**Claude Design pass 3?**~~ — **Answered (pass 3):** Pass 3 done 2026-08-17 → `DESIGN.md` v1.3 §12. No pass 4 needed before build.
43. **Workroom email opt-in copy + privacy page line** — draft when Phase 2 starts.

## Design pass 3 — **landed 2026-08-17** (`design/claude-design-export/pass-3/`, `DESIGN.md` v1.3 §12)
Answers to Claude Design's open items (build decisions, no re-pass): view counts refresh hourly (data model) · general mentions tagged with the ODSENS wordmark chip · suggested-mention source = YouTube search per project title (v1.5) · one Discord webhook in v1 (a second mod-channel webhook is a field, not a redesign) · leaderboard linking = hashed-email match (decided Q33) · **nav shows "Commissions" only when Phase 2 ships**.
44. **Allay render** — Oliver to draw a pixel allay (Minecraft mob; same fan-content footing as the rest) for the email header (28px), Discord bot avatar, and Settings → Notifications. Also needed at build: official YouTube/TikTok/Twitch/Reddit marks (24px) from brand-guideline downloads. *(Oliver)*
45. **Workroom limits** — confirm 25 MB/file, 200 MB/room; **max participants per room** (proposed 6). *(Phase 2)*
46. **Future passes** — tell Claude Design to stop carrying pass-1/2 files forward (each pass re-adds ~12 MB of identical assets). Decide: self-contained snapshots (current) vs. delta-only exports.

## Engineering specs (2026-08-17)
`docs/build/00–05` + `_registry.md` + ADR-0001/0002 (with Amendment A) **FROZEN v1.0 (2026-08-17)** — build starts at S0 in a fresh session (`docs/build/START-BUILD.md`).

## S0 build notes (2026-08-17 → 2026-08-20)
- 2026-08-17 — S0 build started on `feat/S0-scaffold`; ADRs filed: ADR-0003 (CSP `unsafe-inline`), ADR-0004 (`/dev/components`), ADR-0005 (placeholder pages static per 02 RP-16), ADR-0006 (preview env fallback until Supabase Branching is live).
- ~~2026-08-17 — S1.1 open: preview `NEXT_PUBLIC_SITE_URL` strategy (per-branch env vs `VERCEL_BRANCH_URL` derivation) — needed for OAuth `redirectTo` on previews (ADR-0006 D2).~~ — **Answered 2026-08-20:** derived — on preview `NEXT_PUBLIC_SITE_URL = 'https://' + VERCEL_BRANCH_URL` (client: `NEXT_PUBLIC_VERCEL_BRANCH_URL`), no per-branch env step (ADR-0010).
- ~~2026-08-17 — S1.1 heads-up: Next 16 renamed `middleware.ts` → `proxy.ts`; 02 §3 / `_registry.md` say `middleware.ts` — decide by ADR at S1.1.~~ — **Answered 2026-08-20:** `proxy.ts` with the named export `proxy` (ADR-0009).
- 2026-08-20 — S0 session resumed after an interruption; PR opened from `feat/S0-scaffold`.
- 2026-08-20 — S0 gate notes (security-reviewer, spec-drift-reviewer; none blocking): (a) `lib/log.ts` now also redacts Google identity keys (`full_name`, `given_name`, `family_name`, `display_name`, `picture`, `avatar_url`, `phone`) and Discord webhook URLs without `token=` (01 INV-43) — S1.1 `/auth/callback` A3a is the first place a `user` object is in hand; (b) **S1.1 / supabase-ops + keep-docs:** 02 §4 and `supabase/config.toml` `[remotes.production.auth].additional_redirect_urls` allow `https://*.vercel.app/**` — any `*.vercel.app` site could start a PKCE flow against the project; narrow to `https://odsens-git-*-studiobing.vercel.app/**` + exact production hosts when S1.1 wires sign-in (ADR + 02 row) — **done 2026-08-20: ADR-0011**; (c) **upkeep:** GitHub Actions are pinned by major tag (`actions/checkout@v4`, `supabase/setup-cli@v1` + `version: latest`) — consider SHA pins + Dependabot; (d) `.env.example` / 04 SC-10 put a personal email in the Modrinth User-Agent contact — David to decide on a role address (spec change); (e) ADR-0003…0006 are dated 2026-08-17 (decision date) while PR #1 opened 2026-08-20 — left as decision dates; (f) C20 placeholder pages set `alternates.canonical` only and inherit the root OG image — real pages set per-page OG in their slices (02 RP-06).
- 2026-08-20 — **David to decide (design details DESIGN.md does not state; code follows 03 / the pass-3 prototypes for now — say "keep" or name the change, then `keep-docs` adds one DESIGN.md line each; a change = `Kind: design` ADR):** (1) footer links are `--chalk` 15px, hover `--white` (pass-3 frames show both this and `--indigo-lift` 14px); (2) the second action on the 404/error pages is a ghost button with → (03 G-02/G-03), pass-3 draws it as an outlined secondary; (3) 404/error pages render with no nav/footer (02 RP-09 root layout) — pass-3 frames show a slim wordmark bar; (4) phone menu: 15px `--mute` links, active = 3px gold underline, plain burger — pass-3 shows 16px `#C6CEDA`, a 4px gold left bar on the active row and an indigo-filled ✕; (5) the disabled/pending look (`--disabled-fill`/`--disabled-text`) applies to every Button variant, not only primary; (6) geometry not in DESIGN.md: toast `max-width: 360px`, 404/error column `max-width: 560px`, error line `max-width: 38ch`, error `!` glyph 40px (pass-3 34px), 404 title 40/30px (pass-3 26px).
- 2026-08-20 — S0 frontend-gate notes (none blocking): (a) `app/global-error.tsx` is the runtime root boundary, but Next's static `500.html` for hard server failures is its builtin page — `vercel-ops` forces a root error on the preview at S1.10 to confirm what visitors see; 03 G-03 says "inline minimal styles" and the file uses CSS Modules + globals (works; `keep-docs` may relax the wording); (b) the Supabase client chunk runs an `eval`/`Function` feature probe that the CSP blocks (benign, caught by the library) — known under ADR-0003; no CSP `report-to` until it is gone; (c) Lighthouse lab LCP on `/` is 3.0 s mobile-simulated with the footer line as LCP element — re-measure at S1.2 when the hero exists (❌ then if > 2.5 s); (d) **David:** there is no favicon (`/favicon.ico` 404s on every page) and DESIGN.md §10 is silent — OK to use the OddSense avatar (`assets/brand/avatar`) as `app/icon.png`? (design call; one DESIGN.md line).

## S1.1 build notes (2026-08-20)
- 2026-08-20 — S1.1 Accounts build started on `feat/S1.1-accounts`; ADRs filed: ADR-0009 (middleware file is `proxy.ts`), ADR-0010 (preview env from the persistent `staging` Supabase branch, `NEXT_PUBLIC_SITE_URL` derived from `VERCEL_BRANCH_URL`; supersedes ADR-0006), ADR-0011 (OAuth redirect allow-list narrowed to `https://odsens-git-*-studiobing.vercel.app/**`), ADR-0012 (`HASH_SECRET` boot-required — 9 names), ADR-0013 (`runAction` wrapper + `AuthError` export, `safeNext` in `lib/validation/next.ts`), ADR-0014 (`ProfilePanel` island on `/profile`, `handle_changed_at` in the own-row select, `AdminShell.viewer.avatarUrl?`, `GoogleSignInButton` `data-variant`), ADR-0015 (`profiles` mutations on other users' rows = service client only — an admin JWT cannot reach other rows, so 05 T-RLS-8/9 admin = D and T-RLS-5 admin = own row; schema as built), ADR-0016 (`/auth/callback` imports the admin client for the `email_hash` stamp — the one non-API `app/**` exception in 01 INV-14/INV-84; the INV-45/INV-46 greps exclude that file). Addenda 2026-08-20: ADR-0009 (`proxy.ts` imports `@supabase/ssr` directly — INV-13/INV-85 exception), ADR-0013 (`<actionName>Input` schemas live in `lib/actions/accounts.schema.ts` — Next 16 allows only async-function exports from a `'use server'` file), ADR-0014 (gate round 1, frontend #9: `ViewerProvider` publishes through a module-level external store + `useSyncExternalStore` instead of a React context, so a still-dehydrated `loading.tsx` boundary is never marked and `/profile` hydrates in place), ADR-0015 (gate round 1, security #16: `profiles.avatar_path` is constrained to the owner's folder by the CHECK `profiles_avatar_path_own` — migration `20260820120400_profiles_avatar_path_check.sql` — and every service-role Storage delete re-checks ownership via `lib/files.ts` `isOwnAvatarPath`; the `site_settings` singleton is created by migration `20260820120500_site_settings_default_row.sql`, not only by seed.sql).
- 2026-08-20 — 05 T-RLS-132 said the seed `moderation_mode` is `'hold_first_time'`; SEED-1, 00 S1.1 and 04 §1.3 seed `'auto'` and the test asserts `'auto'` — doc typo, cell corrected with a dated 05 §12 note (prose fix, no ADR).
- 2026-08-20 — `HASH_SECRET` set in Vercel **production + preview** and David's `.env` — **done 2026-08-20** (generated the same day; names only recorded here; ADR-0012).
- 2026-08-20 — local CI green (lint · unit 330 · db 367 · build + bundle-secret grep · e2e 56/56) before PR; zod removed from the client bundle (ADR-0008 D3 / ADR-0013)
47. ~~**Google sign-in on Supabase preview branches** *(David)* — each ephemeral preview branch has its own Auth callback (`https://<branch-ref>.supabase.co/auth/v1/callback`) that Google's OAuth client must list, and that list cannot be pushed from `config.toml`. On a preview, the sign-in *start* (the redirect to the branch's `/auth/v1/authorize?provider=google`) is verifiable; the Google consent leg is not unless Google knows that branch's callback. Options: (a) add one Google-console redirect URI per PR branch when a preview needs the full round-trip; (b) keep a persistent `staging` Supabase branch, register its callback once, and point `[remotes.staging]` at it; (c) verify the Google consent leg on production after merge only (Google already knows production's callback). **Foreman recommendation: (c), with (a) on demand for a PR that needs it; revisit at S1.10.** Nothing in code changes either way.~~ — **Answered 2026-08-20 (David):** option (b) — a persistent `staging` Supabase branch (git branch `staging`; `[remotes.staging]` in `supabase/config.toml`; its Auth callback registered once in the Google console) serves every preview, and automatic per-PR branching is turned off (ADR-0010).
- 2026-08-20 — **David to decide (design details DESIGN.md does not state; code follows the pass-3 frames for now — say "keep" or name the change, then `keep-docs` adds one DESIGN.md line each; a change = `Kind: design` ADR, D4 = `Kind: security` ADR; recorded by ADR-0014):** (D1) delete-account inline confirm copy "Delete your account? Your handle, picture and comments go with it." → "Delete it" / "Keep it" (follows the §11.2 comment-delete pattern) — keep?; (D2) avatar crop step copy "CROP IT SQUARE" + "Drag to move. Everything outside the box gets cut." (from the pass-3 frame) and the crop geometry 200px viewport / 150px gold box — keep?; (D3) the chalk "Continue with Google" button carries the pass-3 `4px 4px 0 --ink-deep` block shadow + hover lift like primary buttons (DESIGN.md §5 names only the chalk fill + dark text) — keep the shadow, or flat?; (D4) signing in from the admin gate lands on the home page (the RP-20 rule that blocks `/admin` as a return target); admins then use the profile menu's Admin item — OK, or should the gate return straight to `/admin`?; (D5) the profile page's "what we store" footer sentence and the picture-row hint ("Optional. Blank is a fine look.") — wording OK?
- 2026-08-20 — **S1.1 deploy-checker PASS on the preview** (staging branch wired, site URL derived, headers + bundle clean). Follow-ups for `vercel-ops` (David confirms each, one line): (a) `NOTIFY_FROM_EMAIL` exists twice in Vercel (a Preview-only row + a Prod/Prev/Dev row) — remove the duplicate; (b) the Supabase integration added production-only `POSTGRES_*`, `SUPABASE_JWT_SECRET`, `SUPABASE_SECRET_KEY` rows the app never reads — prune the sensitive ones (`POSTGRES_PASSWORD`, `SUPABASE_JWT_SECRET`) to shrink the blast radius; (c) `site_settings` has no row on the persistent `staging` branch (seed.sql runs only at branch creation) — a default-row migration (`insert … (id) values (1) on conflict do nothing`) lands in S1.1 so production + staging always have the singleton; seed.sql still sets `kofi_page`/`owner_profile_id` locally — **done 2026-08-20:** migration `20260820120500_site_settings_default_row.sql` (ADR-0015).
- 2026-08-20 — **Systemic gap (security-reviewer gate round 1 #16; fixed for avatars by the ADR-0015 addendum):** an owner-writable `*_path` column is a pointer into Storage, and a service-role delete that trusts it lets a user aim it at another user's object. Rule going forward: every owner-writable `*_path` column gets the same CHECK-to-owner pattern as `profiles_avatar_path_own` (the path must start with the owner's own id) plus an ownership re-check before any service-role Storage delete — `supabase-ops` applies it to skins / art at S1.7.
- 2026-08-20 — **Security gate round 2 (PASS) — open items, David to decide later:** (2) `images.remotePatterns` for the Supabase host has no `pathname` restriction (`/storage/v1/object/public/**`), so any path on that host can reach `/_next/image` — tightening is a 01 INV-54 edit → later ADR (`vercel-ops`); (3) 04 SC-05 scopes the banned check to insert actions, so a banned user can still rename (`updateProfile`) and change their picture — intended, or should bans freeze the handle too (ban-evasion by rename on existing comments)? A change = `Kind: security` ADR (`backend-robustness`) before S1.4.
- 2026-08-21 — **David on merge (S1.1):** banned accounts may delete themselves (follow-up PR right after S1.1: `deleteAccount` allowed for banned callers + a Delete account control on `/banned`; ADR-0019 follow-up) — **built 2026-08-27** (ADR-0021, `fix/S1.1-banned-delete`: `requireOnboarded({allowBanned:true})` passed only by `deleteAccount`; `BannedDelete` on the slab, onboarded banned only); look-alike handles (`0ddsense`, `admin1`) stay allowed for now (security round-3b advisory (5)). Follow-ups also logged from the last gate round: backend ⚠32 (structured detail on our own thrown errors in `runAction` logs), frontend ⚠14 (`app/icon.*` so `/favicon.ico` stops 404ing), schema row 20 (before the owner bootstrap run `select id, handle from public.profiles where handle is not null and public.is_reserved_handle(handle::text);` and free any squat).
- 2026-08-21 — **David's S1.1 preview review (decided; code + ADRs in PR #2):** (1) no Skip button on onboarding — leaving the picture empty and pressing DONE is the skip; DONE is gated on the handle only (ADR-0017); (2) after DONE the page now leaves with a document navigation — the old soft navigation reused a prefetched "go home → back to /welcome" answer from before the handle existed (ADR-0017), and the proxy no longer redirects Server Action POSTs — a second DONE used to be re-posted to `/` and crash the page (ADR-0009 addendum); (3) profile menu = Your profile · [Admin] · Sign out (ADR-0018); (4) deleting the account also signs the browser out so the header drops the name/picture at once (bug fix); (5) a banned user's Google sign-in lands on a banned page with nothing but Sign out (ADR-0019 — answers the security open item (3) above); (6) D1–D5: keep all. Oliver claims `oddsense` by the documented owner SQL (see the S1.1 notes below / `.claude/skills/supabase-ops/SKILL.md` "Owner bootstrap").
- 2026-08-20 — **Gate round 2 non-blocking follow-ups (owner `backend-robustness` / `test-engineer`; no ADR):** (a) `deleteAccount` deletes the avatar object before `auth.admin.deleteUser` — swap the order so a failed `deleteUser` cannot leave a dangling `avatar_path` behind the 1/day limit; (b) `proxy.ts` M4 ignores the select `error` — a PostgREST blip reads as a null handle and bounces onboarded users to `/welcome`; pass through (M8) on error; (c) `updateProfile` with handle + avatar in one call commits the rename before the upload (unreachable from `ProfilePanel`'s two forms); (d) no test exercises `deleteAvatarQuietly`'s two warn branches (`avatar_path_not_own`, `avatar_cleanup_failed`); (e) frontend ⚠: `AdminNav` prefetches the six admin routes that ship in S1.2+ (six RSC 404s on `/admin` load) — resolves as the routes land, 03 §2.10 kept as specified.
- 2026-08-21 — **David (S1.1 preview review):** the profile menu's "Change handle" / "Change picture" items were redundant with "Your profile" (all three open `/profile`) — the menu is now Your profile · [Admin — role ≥ moderator] · Sign out (ADR-0018, `Kind: design`; DESIGN.md v1.4 §11.1; 03 §2.5; 02 RP-12 + `/profile` row; T-E2E-32 updated). Bug fixed in the same round, no ADR (ADR-R9): deleting the account left the old handle + picture in the nav until a reload — `deleteAccount` clears the server cookies but the browser Supabase client kept its session; `ProfilePanel` now dispatches `odsens:viewer-signed-out` and `ViewerProvider` publishes `anon` + signs the browser client out locally (03 C-16a `ViewerProvider` row, prose).
- 2026-08-21 — **Security gate round 3 systemic gaps (fixed for H3 by ADR-0020):** (1) every rule implemented in an RPC/action must also bind the direct RLS write path (DB twin + T-RLS cell) — done for H3 by ADR-0020 (`is_reserved_handle()` + `profiles_guard`, migration `20260821090000_profiles_guard_reserved_and_banned.sql`; the browser holds the anon key + its own JWT and can PATCH its own row without any action); (2) `profiles_guard` is now ban-aware — a banned account's own-row write is 42501 (ADR-0020); (3) proxy M4b exempts `/api/*` — every future `/api/**` handler needs its own banned check (`backend-robustness`).
- 2026-08-21 — **S1.1 MERGED:** PR #2 squash-merged as `464d429` on `main`, tag `v0.2`; ADR-0009..0020 Accepted; gate round 3b all seven PASS. Production verified same day: Vercel Ready for `464d429`, `https://odsens.com` 308 → `https://www.odsens.com` 200, all 8 migrations applied on production Supabase by the GitHub integration, Google sign-in live. Note (docs corrected 2026-08-27): the production **custom domain is public** — Vercel Deployment Protection (Standard) guards `*.vercel.app` URLs and previews, not an attached custom domain, so the "behind Deployment Protection until S1.10" line was never true once odsens.com was attached; S1.10 keeps only the launch checks (DNS, Sentry, analytics, seeding). Still open from the merge: David's one real Google sign-in on production, then Oliver's first sign-in + the owner-bootstrap SQL (`supabase-ops` "Owner bootstrap"; run the squat-check SQL first).

## S1.2 build notes (2026-08-27)
- 2026-08-27 — S1.2 Projects (synced) Session-A build pass on `feat/S1.2-projects` (based on `fix/S1.1-banned-delete`; PR auto-retargets to main when #3 merges). ADR filed: ADR-0022 (`project_is_visible(uuid)` — the data-model §4 visibility predicate as one definer helper for the S1.2 SELECT policies; inline cross-referencing policies recurse 42P17). F-2 prose fixes: 04 `JobSummary.skipped` string|number (doc-internal reconciliation), 04 SC-07 `revalidateTag(tag,'max')` (Next 16.3 second argument), 03 §9 `--text-stat` 34px. Registry add-first additions: `lib/validation/filters.ts`, `lib/data/admin.ts`, `project_is_visible`.
- 2026-08-27 — **Spec tension for the gates (not resolved in code):** 02 §1.3 gives moderators admin-page read access, but 05's frozen T-RLS matrix denies moderators the hidden-project and `sync_runs` selects — the admin pages read through the cookie client under RLS, so a moderator sees published-only rows and no sync table (controls disabled either way; actions refuse server-side). If Session-B gates or David want full moderator visibility, that is an ADR + RLS change, not a page change.
- 2026-08-27 — `CURSEFORGE_API_KEY` still not obtained (docs/questions.md setup list): CF adapter/action/job ship verified against fixtures; `setProjectLink` answers `upstream_error` "CurseForge key not configured" until the key lands in Vercel + `.env` (AC8's CF leg then verifiable on the preview).

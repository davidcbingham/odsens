{
  "name": "odsens",
  "version": "0.1.0",
  "private": true,
  "description": "odsens.com — OddSense's portfolio site",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/wait-for-schema.mjs && next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . && prettier --check . && tsc --noEmit && node scripts/contrast.mjs --check styles/ && node scripts/check-fixtures.mjs && node scripts/check-test-ids.mjs && node scripts/check-client-islands.mjs",
    "format": "prettier --write .",
    "contrast": "node scripts/contrast.mjs",
    "test": "pnpm test:unit && pnpm test:db",
    "test:unit": "vitest run --project unit",
    "test:db": "vitest run --project db",
    "test:e2e": "playwright test",
    "test:coverage": "vitest run --coverage",
    "email": "email dev --dir emails/preview --port 3300"
  },
  "dependencies": {
    "@react-email/components": "^1.0.12",
    "@supabase/ssr": "^0.12.4",
    "@supabase/supabase-js": "^2.112.3",
    "next": "^16.3.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-markdown": "^10.1.0",
    "rehype-sanitize": "^6.0.0",
    "remark-gfm": "^4.0.1",
    "server-only": "^0.0.1",
    "sharp": "^0.35.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "@react-email/ui": "^6.9.3",
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^9.39.5",
    "eslint-config-next": "^16.3.1",
    "prettier": "^3.9.6",
    "react-email": "^6.9.3",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
